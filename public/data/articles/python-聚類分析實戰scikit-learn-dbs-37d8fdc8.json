{"id":"37d8fdc8-a2ed-40c2-9a8a-5e1f9d1565be","title":"Python 聚類分析實戰：scikit-learn DBSCAN/HDBSCAN 完整實作指南與實際資料集案例分析（application 階段）","slug":"python-聚類分析實戰scikit-learn-dbs-37d8fdc8","category":"Claude Code","categorySlug":"claude","tags":["Python","聚類分析","DBSCAN","HDBSCAN","scikit-learn","異常偵測","實作指南","application","程式碼範例","資料集案例","2026","daily-digest","技術研究"],"updatedAt":"2026-04-14","excerpt":"Python 聚類分析實戰：scikitlearn DBSCAN/HDBSCAN 完整實作指南與實際資料集案例分析 承接「foundation（基礎概念）+ mechanism（演算法原理）」知識，本文進入「application（實作應用","readingMin":27,"isNew":false,"html":"<h1 id=\"python-聚類分析實戰-scikit-learn-dbscan-hdbsca\">Python 聚類分析實戰：scikit-learn DBSCAN/HDBSCAN 完整實作指南與實際資料集案例分析</h1>\n<blockquote><p>承接「foundation（基礎概念）+ mechanism（演算法原理）」知識，本文進入「application（實作應用）」階段。</p>\n</blockquote>\n<h2 id=\"執行摘要\">執行摘要</h2>\n<p>本文提供 Python 環境下 DBSCAN 和 HDBSCAN 聚類演算法的完整實作指南。DBSCAN（Density-Based Spatial Clustering of Applications with Noise）是一種密度基聚類演算法，能自動識別任意形狀的聚類並標記噪音點，HDBSCAN 則是其階層式改良版本，可處理變化密度的資料集。本文涵蓋環境準備、sklearn.cluster.DBSCAN 與 hdbscan.HDBSCAN 的 API 使用、參數調整技巧（eps 選擇、min_samples 經驗法則）、特徵縮放的必要性、Iris 資料集實作範例、聚類評估指標（Silhouette Score、DBCV）、異常檢測應用、以及 DBSCAN vs HDBSCAN 選擇指南。所有程式碼範例均可直接執行，並附完整註解說明。</p>\n<hr>\n<h2 id=\"一-環境準備與套件安裝\">一、環境準備與套件安裝</h2>\n<h3 id=\"1-1-必要套件\">1.1 必要套件</h3>\n<p>Python 環境下實作 DBSCAN 和 HDBSCAN 需要以下核心套件：</p>\n<ul>\n<li><strong>scikit-learn</strong>：提供 sklearn.cluster.DBSCAN 官方實作 [來源：scikit-learn 官方文件, 2024]</li>\n<li><strong>hdbscan</strong>：HDBSCAN 官方套件，與 scikit-learn 介面相容 [來源：HDBSCAN 官方文件, 2024]</li>\n<li><strong>numpy</strong>：數值運算基礎</li>\n<li><strong>pandas</strong>：資料處理與分析</li>\n<li><strong>matplotlib</strong>：視覺化工具</li>\n<li><strong>seaborn</strong>：進階視覺化</li>\n</ul>\n<p>安裝指令：</p>\n<pre><code class=\"language-bash\">pip install scikit-learn hdbscan numpy pandas matplotlib seaborn</code></pre>\n<p>對於需要評估聚類品質的進階使用者，可額外安裝：</p>\n<pre><code class=\"language-bash\">pip install validclust  # 提供 DBCV 指標實作</code></pre>\n<h3 id=\"1-2-匯入與初始化\">1.2 匯入與初始化</h3>\n<p>完整的匯入區段範例：</p>\n<pre><code class=\"language-python\"># 聚類演算法\nfrom sklearn.cluster import DBSCAN\nimport hdbscan\n\n# 資料處理\nimport numpy as np\nimport pandas as pd\nfrom sklearn.datasets import load_iris\nfrom sklearn.preprocessing import StandardScaler\n\n# 視覺化\nimport matplotlib.pyplot as plt\nimport seaborn as sns\n\n# 評估指標\nfrom sklearn.metrics import silhouette_score\n\n# 設定繪圖風格\nsns.set_style('whitegrid')\nplt.rcParams['figure.figsize'] = (10, 6)</code></pre>\n<hr>\n<h2 id=\"二-dbscan-實作指南\">二、DBSCAN 實作指南</h2>\n<h3 id=\"2-1-基本用法\">2.1 基本用法</h3>\n<p>sklearn.cluster.DBSCAN 提供標準化的聚類介面，核心 API 使用如下 [來源：scikit-learn DBSCAN API 文件, 2024]：</p>\n<pre><code class=\"language-python\">from sklearn.cluster import DBSCAN\n\n# 建立 DBSCAN 聚類器\ndbscan = DBSCAN(\n    eps=0.5,          # 鄰域半徑（epsilon）\n    min_samples=5,    # 最小樣本數\n    metric='euclidean',  # 距離度量（預設歐式距離）\n    algorithm='auto'  # 演算法選擇（auto, ball_tree, kd_tree, brute）\n)\n\n# 訓練模型並預測聚類標籤\nlabels = dbscan.fit_predict(X)\n\n# 取得核心樣本索引\ncore_samples_mask = np.zeros_like(labels, dtype=bool)\ncore_samples_mask[dbscan.core_sample_indices_] = True</code></pre>\n<p>DBSCAN 將資料點分為三類：</p>\n<ol>\n<li><strong>核心點（Core Points）</strong>：在 eps 鄰域內至少有 min_samples 個點</li>\n<li><strong>邊界點（Border Points）</strong>：在核心點的鄰域內，但本身不是核心點</li>\n<li><strong>噪音點（Noise Points）</strong>：既不是核心點也不是邊界點，label = -1</li>\n</ol>\n<h3 id=\"2-2-參數調整\">2.2 參數調整</h3>\n<p>DBSCAN 的效能高度依賴兩個參數的選擇 [來源：DataCamp DBSCAN 指南, 2026]：</p>\n<p><strong>eps（epsilon, 鄰域半徑）選擇方法：</strong></p>\n<p>最常用的方法是繪製 k-distance 圖表找出「膝點（knee point）」[來源：Mastering DBSCAN with Scikit-learn, 2024]：</p>\n<pre><code class=\"language-python\">from sklearn.neighbors import NearestNeighbors\n\n# 計算每個點到第 k 個最近鄰的距離\nk = 4  # min_samples 值\nneighbors = NearestNeighbors(n_neighbors=k)\nneighbors_fit = neighbors.fit(X)\ndistances, indices = neighbors_fit.kneighbors(X)\n\n# 排序距離並繪圖\ndistances = np.sort(distances[:, k-1], axis=0)\nplt.plot(distances)\nplt.ylabel('k-NN Distance')\nplt.xlabel('Data Points (sorted by distance)')\nplt.title('k-Distance Graph')\nplt.axhline(y=0.5, color='r', linestyle='--', label='Suggested eps=0.5')\nplt.legend()\nplt.show()</code></pre>\n<p>膝點位置對應的距離值即為建議的 eps 值。</p>\n<p><strong>min_samples 經驗法則：</strong></p>\n<p>根據實務經驗 [來源：Mastering DBSCAN with Scikit-learn, 2024]：</p>\n<ul>\n<li>一般建議：<code>min_samples ≥ 資料維度 × 2</code></li>\n<li>低維資料（2D-3D）：min_samples = 4 或 5</li>\n<li>高維資料：可增加至 10-20</li>\n<li>資料較多噪音時：增加 min_samples 提高聚類穩健性</li>\n</ul>\n<h3 id=\"2-3-特徵縮放\">2.3 特徵縮放</h3>\n<p>特徵縮放對 DBSCAN 結果至關重要，因為演算法依賴距離計算 [來源：scikit-learn DBSCAN 文件, 2024]。不同量級的特徵會導致某些維度主導距離計算，產生錯誤的聚類結果。</p>\n<p><strong>StandardScaler 使用範例：</strong></p>\n<pre><code class=\"language-python\">from sklearn.preprocessing import StandardScaler\n\n# 建立縮放器\nscaler = StandardScaler()\n\n# 訓練並轉換資料（訓練集）\nX_scaled = scaler.fit_transform(X)\n\n# 僅轉換資料（測試集）\nX_test_scaled = scaler.transform(X_test)\n\n# 檢查縮放結果\nprint(f&quot;原始資料範圍: {X.min(axis=0)} ~ {X.max(axis=0)}&quot;)\nprint(f&quot;縮放後範圍: {X_scaled.min(axis=0)} ~ {X_scaled.max(axis=0)}&quot;)\nprint(f&quot;縮放後均值: {X_scaled.mean(axis=0)}&quot;)  # 應接近 0\nprint(f&quot;縮放後標準差: {X_scaled.std(axis=0)}&quot;)  # 應接近 1</code></pre>\n<p>常用的縮放方法 [來源：DBSCAN Python 教學, 2024]：</p>\n<ul>\n<li><strong>StandardScaler</strong>：標準化至均值=0、標準差=1（適用於常態分佈資料）</li>\n<li><strong>MinMaxScaler</strong>：縮放至 [0, 1] 範圍（適用於有明確界限的資料）</li>\n<li><strong>RobustScaler</strong>：使用中位數和四分位距（適用於有極端值的資料）</li>\n</ul>\n<h3 id=\"2-4-iris-資料集實作範例\">2.4 Iris 資料集實作範例</h3>\n<p>以下是完整的端到端實作範例，使用經典的 Iris 資料集 [來源：DBSCAN Clustering with HDBSCAN: Python Tutorial with Iris Dataset, 2024]：</p>\n<pre><code class=\"language-python\">import numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nimport seaborn as sns\nfrom sklearn.datasets import load_iris\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.cluster import DBSCAN\nfrom sklearn.metrics import silhouette_score\n\n# 1. 載入資料\niris = load_iris()\nX = iris.data\ny_true = iris.target  # 真實標籤（僅用於驗證）\n\n# 2. 特徵縮放\nscaler = StandardScaler()\nX_scaled = scaler.fit_transform(X)\n\n# 3. 建立並訓練 DBSCAN\ndbscan = DBSCAN(eps=0.6, min_samples=4)\nlabels = dbscan.fit_predict(X_scaled)\n\n# 4. 分析結果\nn_clusters = len(set(labels)) - (1 if -1 in labels else 0)\nn_noise = list(labels).count(-1)\n\nprint(f&quot;聚類數量: {n_clusters}&quot;)\nprint(f&quot;噪音點數量: {n_noise}&quot;)\nprint(f&quot;核心樣本數量: {len(dbscan.core_sample_indices_)}&quot;)\n\n# 5. 評估聚類品質（排除噪音點）\nif n_clusters &gt; 1:\n    mask = labels != -1\n    score = silhouette_score(X_scaled[mask], labels[mask])\n    print(f&quot;Silhouette Score: {score:.3f}&quot;)\n\n# 6. 視覺化（使用前兩個主成分）\nplt.figure(figsize=(12, 5))\n\n# 子圖 1：DBSCAN 聚類結果\nplt.subplot(1, 2, 1)\nunique_labels = set(labels)\ncolors = [plt.cm.Spectral(each) for each in np.linspace(0, 1, len(unique_labels))]\n\nfor k, col in zip(unique_labels, colors):\n    if k == -1:\n        col = [0, 0, 0, 1]  # 黑色標記噪音點\n\n    class_member_mask = (labels == k)\n    xy = X_scaled[class_member_mask]\n    plt.plot(xy[:, 0], xy[:, 1], 'o', markerfacecolor=tuple(col),\n             markeredgecolor='k', markersize=8, label=f'Cluster {k}')\n\nplt.title(f'DBSCAN Clustering (eps={dbscan.eps}, min_samples={dbscan.min_samples})')\nplt.xlabel('Sepal Length (scaled)')\nplt.ylabel('Sepal Width (scaled)')\nplt.legend()\n\n# 子圖 2：真實標籤（對照用）\nplt.subplot(1, 2, 2)\nscatter = plt.scatter(X_scaled[:, 0], X_scaled[:, 1], c=y_true, cmap='viridis')\nplt.colorbar(scatter)\nplt.title('Ground Truth Labels')\nplt.xlabel('Sepal Length (scaled)')\nplt.ylabel('Sepal Width (scaled)')\n\nplt.tight_layout()\nplt.show()\n\n# 7. 建立結果 DataFrame\nresults_df = pd.DataFrame({\n    'sepal_length': X[:, 0],\n    'sepal_width': X[:, 1],\n    'petal_length': X[:, 2],\n    'petal_width': X[:, 3],\n    'cluster': labels,\n    'true_label': y_true\n})\n\nprint(&quot;\\n前 10 筆聚類結果:&quot;)\nprint(results_df.head(10))</code></pre>\n<p><strong>執行結果說明：</strong></p>\n<p>使用 Iris 資料集時，eps=0.6 和 min_samples=4 通常能產生 2-3 個主要聚類 [來源：DBSCAN Python Tutorial with Iris Dataset, 2024]。由於 Iris 資料集的 setosa 類別與其他兩類在特徵空間中有明顯分離，DBSCAN 能有效識別出這個聚類。</p>\n<hr>\n<h2 id=\"三-hdbscan-實作指南\">三、HDBSCAN 實作指南</h2>\n<h3 id=\"3-1-基本用法\">3.1 基本用法</h3>\n<p>HDBSCAN（Hierarchical DBSCAN）提供與 scikit-learn 相容的介面，但參數更簡化 [來源：HDBSCAN 官方文件基本用法, 2024]：</p>\n<pre><code class=\"language-python\">import hdbscan\n\n# 建立 HDBSCAN 聚類器\nclusterer = hdbscan.HDBSCAN(\n    min_cluster_size=5,       # 最小聚類大小（唯一必要參數）\n    min_samples=None,         # 最小樣本數（預設與 min_cluster_size 相同）\n    cluster_selection_epsilon=0.0,  # 額外的 epsilon 閾值（預設 0）\n    metric='euclidean',       # 距離度量\n    cluster_selection_method='eom'  # 聚類選擇方法（eom 或 leaf）\n)\n\n# 訓練模型\nclusterer.fit(X_scaled)\n\n# 取得聚類標籤\nlabels = clusterer.labels_\n\n# 取得機率分數（soft clustering）\nprobabilities = clusterer.probabilities_</code></pre>\n<p>HDBSCAN 的關鍵優勢在於簡單的 API：只需設定 <code>min_cluster_size</code> 參數，無需手動調整 eps [來源：HDBSCAN 官方文件, 2024]。</p>\n<h3 id=\"3-2-參數調整\">3.2 參數調整</h3>\n<p><strong>min_cluster_size（最小聚類大小）：</strong></p>\n<p>這是 HDBSCAN 最重要的參數，決定形成聚類所需的最小點數。選擇建議：</p>\n<ul>\n<li>小值（5-10）：產生更多、更小的聚類</li>\n<li>大值（15-30）：產生較少、較大的聚類</li>\n<li>經驗法則：觀察資料分佈，估計最小有意義聚類的大小</li>\n</ul>\n<p><strong>soft clustering（軟聚類）：</strong></p>\n<p>HDBSCAN 為每個資料點提供 0.0-1.0 的成員分數，表示該點屬於聚類的信心程度 [來源：HDBSCAN 基本用法文件, 2024]：</p>\n<pre><code class=\"language-python\"># 取得成員分數\nprobabilities = clusterer.probabilities_\n\n# 識別低信心點\nlow_confidence_mask = probabilities &lt; 0.5\nlow_confidence_points = X[low_confidence_mask]\n\nprint(f&quot;低信心點數量: {low_confidence_points.shape[0]}&quot;)\n\n# 視覺化成員分數\nplt.scatter(X_scaled[:, 0], X_scaled[:, 1], c=probabilities, cmap='viridis')\nplt.colorbar(label='Membership Probability')\nplt.title('HDBSCAN Membership Scores')\nplt.show()</code></pre>\n<h3 id=\"3-3-與-dbscan-比較\">3.3 與 DBSCAN 比較</h3>\n<p>HDBSCAN 相較於 DBSCAN 的三大優勢 [來源：HDBSCAN vs DBSCAN - Daily Dose of Data Science, 2024]：</p>\n<p><strong>1. 處理變化密度的聚類：</strong></p>\n<p>DBSCAN 假設所有聚類具有相似的密度，使用單一 eps 值。HDBSCAN 則在不同 epsilon 值下執行 DBSCAN 並整合結果，能自動適應密度變化的資料集 [來源：Understanding HDBSCAN - Arize, 2024]。</p>\n<pre><code class=\"language-python\"># 示範：變化密度資料集\nfrom sklearn.datasets import make_blobs\n\n# 建立兩個密度不同的聚類\nX1, _ = make_blobs(n_samples=200, centers=1, cluster_std=0.3, random_state=42)\nX2, _ = make_blobs(n_samples=200, centers=1, cluster_std=1.5, random_state=42)\nX2 = X2 + 5  # 移動至不同位置\nX_varied = np.vstack([X1, X2])\n\n# DBSCAN 結果（單一 eps）\ndbscan = DBSCAN(eps=0.5, min_samples=5)\ndbscan_labels = dbscan.fit_predict(X_varied)\n\n# HDBSCAN 結果（自適應 eps）\nhdbscan_clusterer = hdbscan.HDBSCAN(min_cluster_size=10)\nhdbscan_clusterer.fit(X_varied)\nhdbscan_labels = hdbscan_clusterer.labels_\n\n# 比較結果（HDBSCAN 能同時識別兩個密度不同的聚類）</code></pre>\n<p><strong>2. 無需手動調整 eps 參數：</strong></p>\n<p>DBSCAN 的 eps 選擇需要 k-distance 圖表分析，容易受主觀判斷影響。HDBSCAN 僅需 min_cluster_size，參數調整更直觀 [來源：HDBSCAN vs DBSCAN, 2024]。</p>\n<p><strong>3. 可預測新資料點：</strong></p>\n<p>HDBSCAN 支援 <code>approximate_predict()</code> 方法，可為新資料點分配聚類標籤，DBSCAN 則不支援此功能 [來源：HDBSCAN vs DBSCAN, 2024]：</p>\n<pre><code class=\"language-python\"># 訓練 HDBSCAN\nclusterer = hdbscan.HDBSCAN(min_cluster_size=5)\nclusterer.fit(X_train)\n\n# 預測新資料點\nnew_labels, new_probabilities = hdbscan.approximate_predict(clusterer, X_new)</code></pre>\n<p><strong>計算複雜度差異：</strong></p>\n<p>HDBSCAN 的時間複雜度略高於 DBSCAN（O(n log n) vs O(n log n) with optimized implementation），但對於中小型資料集（&lt; 100,000 點）差異不顯著。</p>\n<h3 id=\"3-4-實作範例\">3.4 實作範例</h3>\n<p>完整的 HDBSCAN 實作流程：</p>\n<pre><code class=\"language-python\">import hdbscan\nimport matplotlib.pyplot as plt\nimport seaborn as sns\nfrom sklearn.datasets import load_iris\nfrom sklearn.preprocessing import StandardScaler\n\n# 1. 載入與前處理\niris = load_iris()\nX = iris.data\nscaler = StandardScaler()\nX_scaled = scaler.fit_transform(X)\n\n# 2. 建立並訓練 HDBSCAN\nclusterer = hdbscan.HDBSCAN(\n    min_cluster_size=5,\n    min_samples=3,\n    cluster_selection_method='eom'\n)\nclusterer.fit(X_scaled)\n\n# 3. 取得結果\nlabels = clusterer.labels_\nprobabilities = clusterer.probabilities_\n\n# 4. 分析\nn_clusters = len(set(labels)) - (1 if -1 in labels else 0)\nn_noise = list(labels).count(-1)\n\nprint(f&quot;聚類數量: {n_clusters}&quot;)\nprint(f&quot;噪音點數量: {n_noise}&quot;)\nprint(f&quot;平均成員分數: {probabilities.mean():.3f}&quot;)\n\n# 5. 視覺化 condensed tree\nclusterer.condensed_tree_.plot(select_clusters=True, selection_palette=sns.color_palette())\nplt.title('HDBSCAN Condensed Tree')\nplt.show()\n\n# 6. 視覺化聚類結果\nplt.figure(figsize=(12, 5))\n\n# 子圖 1：聚類標籤\nplt.subplot(1, 2, 1)\nplt.scatter(X_scaled[:, 0], X_scaled[:, 1], c=labels, cmap='viridis')\nplt.colorbar(label='Cluster Label')\nplt.title('HDBSCAN Clustering Results')\nplt.xlabel('Feature 1 (scaled)')\nplt.ylabel('Feature 2 (scaled)')\n\n# 子圖 2：成員分數\nplt.subplot(1, 2, 2)\nplt.scatter(X_scaled[:, 0], X_scaled[:, 1], c=probabilities, cmap='plasma')\nplt.colorbar(label='Membership Probability')\nplt.title('HDBSCAN Membership Scores')\nplt.xlabel('Feature 1 (scaled)')\nplt.ylabel('Feature 2 (scaled)')\n\nplt.tight_layout()\nplt.show()</code></pre>\n<p><strong>condensed tree 視覺化說明：</strong></p>\n<p>condensed tree 是 HDBSCAN 特有的視覺化工具，顯示聚類在不同 lambda（相當於 1/epsilon）值下的階層結構 [來源：How HDBSCAN Works 官方文件, 2024]。圖中水平軸代表 lambda 值，垂直軸代表樣本數量，分支點顯示聚類的分裂與合併過程。</p>\n<hr>\n<h2 id=\"四-聚類結果評估\">四、聚類結果評估</h2>\n<h3 id=\"4-1-silhouette-score\">4.1 Silhouette Score</h3>\n<p>Silhouette Score（輪廓係數）是最常用的聚類評估指標，範圍為 -1 到 +1 [來源：Intrinsic Measures for Clustering Evaluation, 2024]：</p>\n<ul>\n<li><strong>+1</strong>：樣本遠離鄰近聚類，與自身聚類非常匹配</li>\n<li><strong>0</strong>：樣本在兩個聚類的邊界上</li>\n<li><strong>-1</strong>：樣本可能被分配到錯誤的聚類</li>\n</ul>\n<p><strong>計算公式：</strong></p>\n<p>對於樣本 i，Silhouette Score 定義為：</p>\n<pre><code class=\"language-plaintext\">s(i) = (b(i) - a(i)) / max(a(i), b(i))</code></pre>\n<p>其中：</p>\n<ul>\n<li>a(i)：樣本 i 到同聚類其他點的平均距離</li>\n<li>b(i)：樣本 i 到最近聚類所有點的平均距離</li>\n</ul>\n<p><strong>Python 程式碼：</strong></p>\n<pre><code class=\"language-python\">from sklearn.metrics import silhouette_score, silhouette_samples\n\n# 整體 Silhouette Score（排除噪音點）\nmask = labels != -1\noverall_score = silhouette_score(X_scaled[mask], labels[mask])\nprint(f&quot;整體 Silhouette Score: {overall_score:.3f}&quot;)\n\n# 每個樣本的 Silhouette Score\nsample_scores = silhouette_samples(X_scaled[mask], labels[mask])\n\n# 視覺化每個聚類的 Silhouette 分佈\nimport matplotlib.pyplot as plt\nimport numpy as np\n\nfig, ax = plt.subplots()\ny_lower = 10\n\nunique_labels = np.unique(labels[mask])\nfor i, label in enumerate(unique_labels):\n    cluster_scores = sample_scores[labels[mask] == label]\n    cluster_scores.sort()\n\n    size = cluster_scores.shape[0]\n    y_upper = y_lower + size\n\n    color = plt.cm.Spectral(float(i) / len(unique_labels))\n    ax.fill_betweenx(np.arange(y_lower, y_upper), 0, cluster_scores,\n                      facecolor=color, edgecolor=color, alpha=0.7,\n                      label=f'Cluster {label}')\n\n    y_lower = y_upper + 10\n\nax.axvline(x=overall_score, color=&quot;red&quot;, linestyle=&quot;--&quot;, label='Average Score')\nax.set_xlabel(&quot;Silhouette Coefficient&quot;)\nax.set_ylabel(&quot;Cluster&quot;)\nax.set_title(&quot;Silhouette Analysis&quot;)\nax.legend()\nplt.show()</code></pre>\n<p><strong>適用場景：</strong></p>\n<p>Silhouette Score 適用於凸形（convex）聚類，如 K-Means 產生的結果。對於任意形狀的密度基聚類，Silhouette 可能低估聚類品質 [來源：Intrinsic Measures for Clustering, 2024]。</p>\n<h3 id=\"4-2-dbcv-指標\">4.2 DBCV 指標</h3>\n<p>DBCV（Density-Based Clustering Validation）專為密度基聚類設計，考慮聚類的密度變化和噪音點 [來源：Intrinsic Measures for Clustering Evaluation, 2024]。DBCV 評估聚類內部密度連接性和聚類間分離度，範圍為 -1 到 +1，值越高表示聚類品質越好。</p>\n<p><strong>為何 DBCV 優於 Silhouette：</strong></p>\n<ul>\n<li>Silhouette 假設聚類為凸形，無法正確評估任意形狀的聚類</li>\n<li>Silhouette 依賴質心概念，對密度聚類不適用</li>\n<li>DBCV 考慮局部密度變化，更適合評估 DBSCAN/HDBSCAN 結果</li>\n</ul>\n<p><strong>Python 程式碼：</strong></p>\n<pre><code class=\"language-python\">from validclust import dunn\nfrom sklearn.metrics.pairwise import euclidean_distances\n\ndef compute_dbcv(X, labels):\n    &quot;&quot;&quot;\n    計算 DBCV 指標（簡化實作）\n    實務上建議使用 validclust 或 hdbscan 套件提供的實作\n    &quot;&quot;&quot;\n    try:\n        from hdbscan import validity_index\n        return validity_index(X, labels)\n    except ImportError:\n        print(&quot;請安裝 hdbscan 套件以使用 DBCV: pip install hdbscan&quot;)\n        return None\n\n# 計算 DBCV\ndbcv_score = compute_dbcv(X_scaled, labels)\nif dbcv_score is not None:\n    print(f&quot;DBCV Score: {dbcv_score:.3f}&quot;)</code></pre>\n<p><strong>注意事項：</strong></p>\n<p>DBCV 計算較為耗時，對於大型資料集（&gt; 10,000 點）可能需要數秒至數分鐘。若僅需快速評估，Silhouette Score 仍是實務上的常用選擇。</p>\n<h3 id=\"4-3-視覺化方法\">4.3 視覺化方法</h3>\n<p>除了量化指標，視覺化是理解聚類結果的重要工具 [來源：DBSCAN Python 教學, 2024]：</p>\n<p><strong>1. 散佈圖（Scatter Plot）：</strong></p>\n<pre><code class=\"language-python\">import matplotlib.pyplot as plt\nimport seaborn as sns\n\nplt.figure(figsize=(10, 6))\n\n# 使用不同顏色標記聚類\nunique_labels = set(labels)\ncolors = [plt.cm.Spectral(each) for each in np.linspace(0, 1, len(unique_labels))]\n\nfor k, col in zip(unique_labels, colors):\n    if k == -1:\n        col = [0, 0, 0, 1]  # 黑色標記噪音點\n\n    class_member_mask = (labels == k)\n    xy = X_scaled[class_member_mask]\n    plt.plot(xy[:, 0], xy[:, 1], 'o', markerfacecolor=tuple(col),\n             markeredgecolor='k', markersize=6)\n\nplt.title('DBSCAN Clustering Results')\nplt.xlabel('Feature 1')\nplt.ylabel('Feature 2')\nplt.show()</code></pre>\n<p><strong>2. Seaborn Pairplot（多維視覺化）：</strong></p>\n<pre><code class=\"language-python\">import seaborn as sns\nimport pandas as pd\n\n# 建立 DataFrame\ndf = pd.DataFrame(X_scaled, columns=[f'Feature_{i}' for i in range(X_scaled.shape[1])])\ndf['Cluster'] = labels\n\n# 繪製 pairplot\nsns.pairplot(df, hue='Cluster', palette='viridis',\n             plot_kws={'alpha': 0.6}, diag_kind='kde')\nplt.suptitle('DBSCAN Clustering - Pairplot Visualization', y=1.02)\nplt.show()</code></pre>\n<p><strong>3. HDBSCAN Condensed Tree：</strong></p>\n<pre><code class=\"language-python\"># HDBSCAN 特有視覺化\nclusterer.condensed_tree_.plot()\nplt.title('HDBSCAN Condensed Tree')\nplt.show()\n\n# 聚類選擇視覺化\nclusterer.condensed_tree_.plot(select_clusters=True)\nplt.title('HDBSCAN Cluster Selection')\nplt.show()</code></pre>\n<p><strong>4. 3D 視覺化（適用於 3 個主要特徵）：</strong></p>\n<pre><code class=\"language-python\">from mpl_toolkits.mplot3d import Axes3D\n\nfig = plt.figure(figsize=(10, 7))\nax = fig.add_subplot(111, projection='3d')\n\nscatter = ax.scatter(X_scaled[:, 0], X_scaled[:, 1], X_scaled[:, 2],\n                     c=labels, cmap='viridis', s=50)\nax.set_xlabel('Feature 1')\nax.set_ylabel('Feature 2')\nax.set_zlabel('Feature 3')\nplt.colorbar(scatter, label='Cluster Label')\nplt.title('3D DBSCAN Clustering')\nplt.show()</code></pre>\n<hr>\n<h2 id=\"五-異常檢測應用\">五、異常檢測應用</h2>\n<h3 id=\"5-1-原理\">5.1 原理</h3>\n<p>DBSCAN 和 HDBSCAN 將無法歸入任何聚類的資料點標記為噪音（label = -1），這些噪音點可視為異常（outliers）[來源：How to do Anomaly Detection using Machine Learning in Python, 2024]。相較於孤立森林（Isolation Forest）或 One-Class SVM，密度聚類的優勢在於：</p>\n<ul>\n<li>無需事先設定異常比例</li>\n<li>能同時識別正常資料的群組結構</li>\n<li>對任意形狀的資料分佈有較強適應性</li>\n</ul>\n<h3 id=\"5-2-實際案例\">5.2 實際案例</h3>\n<p>以下是工業感測器資料異常檢測的完整範例 [來源：Anomaly Detection using Machine Learning in Python, 2024]：</p>\n<pre><code class=\"language-python\">import numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nfrom sklearn.cluster import DBSCAN\nfrom sklearn.preprocessing import StandardScaler\n\n# 1. 模擬工業感測器資料（溫度、壓力、振動）\nnp.random.seed(42)\n\n# 正常運作資料（3 個感測器，1000 筆）\nnormal_data = np.random.randn(1000, 3) * [10, 5, 2] + [75, 100, 1.5]\n\n# 異常資料（設備故障，50 筆）\nanomalies = np.random.randn(50, 3) * [20, 15, 10] + [120, 150, 8.0]\n\n# 合併資料\nsensor_data = np.vstack([normal_data, anomalies])\n\n# 2. 特徵縮放\nscaler = StandardScaler()\nsensor_scaled = scaler.fit_transform(sensor_data)\n\n# 3. 執行 DBSCAN\ndbscan = DBSCAN(eps=0.5, min_samples=10)\nlabels = dbscan.fit_predict(sensor_scaled)\n\n# 4. 識別異常點\nis_anomaly = (labels == -1)\nanomaly_indices = np.where(is_anomaly)[0]\n\nprint(f&quot;偵測到異常數量: {is_anomaly.sum()}&quot;)\nprint(f&quot;異常比例: {is_anomaly.sum() / len(sensor_data) * 100:.2f}%&quot;)\n\n# 5. 建立結果 DataFrame\nresults_df = pd.DataFrame(sensor_data, columns=['Temperature', 'Pressure', 'Vibration'])\nresults_df['Cluster'] = labels\nresults_df['Is_Anomaly'] = is_anomaly\n\n# 6. 視覺化\nfig = plt.figure(figsize=(15, 5))\n\n# 子圖 1：溫度 vs 壓力\nax1 = fig.add_subplot(131)\nnormal_mask = ~is_anomaly\nax1.scatter(sensor_data[normal_mask, 0], sensor_data[normal_mask, 1],\n            c='blue', alpha=0.5, label='Normal')\nax1.scatter(sensor_data[is_anomaly, 0], sensor_data[is_anomaly, 1],\n            c='red', s=100, marker='x', label='Anomaly')\nax1.set_xlabel('Temperature (°C)')\nax1.set_ylabel('Pressure (kPa)')\nax1.set_title('Temperature vs Pressure')\nax1.legend()\n\n# 子圖 2：壓力 vs 振動\nax2 = fig.add_subplot(132)\nax2.scatter(sensor_data[normal_mask, 1], sensor_data[normal_mask, 2],\n            c='blue', alpha=0.5, label='Normal')\nax2.scatter(sensor_data[is_anomaly, 1], sensor_data[is_anomaly, 2],\n            c='red', s=100, marker='x', label='Anomaly')\nax2.set_xlabel('Pressure (kPa)')\nax2.set_ylabel('Vibration (mm/s)')\nax2.set_title('Pressure vs Vibration')\nax2.legend()\n\n# 子圖 3：異常檢測時序圖\nax3 = fig.add_subplot(133)\nax3.plot(results_df.index, results_df['Temperature'], alpha=0.3, label='Temperature')\nax3.scatter(results_df.index[is_anomaly], results_df['Temperature'][is_anomaly],\n            c='red', s=100, marker='x', label='Detected Anomaly')\nax3.set_xlabel('Sample Index')\nax3.set_ylabel('Temperature (°C)')\nax3.set_title('Anomaly Detection Timeline')\nax3.legend()\n\nplt.tight_layout()\nplt.show()\n\n# 7. 輸出異常報告\nprint(&quot;\\n異常資料點詳細資訊:&quot;)\nprint(results_df[is_anomaly][['Temperature', 'Pressure', 'Vibration']].head(10))\n\n# 8. 統計分析\nprint(&quot;\\n異常點統計摘要:&quot;)\nprint(results_df[is_anomaly][['Temperature', 'Pressure', 'Vibration']].describe())\nprint(&quot;\\n正常點統計摘要:&quot;)\nprint(results_df[~is_anomaly][['Temperature', 'Pressure', 'Vibration']].describe())</code></pre>\n<p><strong>實務應用建議：</strong></p>\n<ol>\n<li><strong>參數調整</strong>：eps 和 min_samples 需根據資料特性調整，過小的 eps 可能將正常變異標記為異常</li>\n<li><strong>閾值設定</strong>：若需控制異常比例，可調整 HDBSCAN 的 <code>outlier_score_</code> 並設定閾值</li>\n<li><strong>時間序列處理</strong>：對於時間序列資料，可使用滑動窗口特徵（如均值、標準差、變化率）作為聚類輸入</li>\n<li><strong>結合領域知識</strong>：異常檢測結果需與設備規格、運作條件結合判斷，避免誤報</li>\n</ol>\n<hr>\n<h2 id=\"六-實務最佳實踐\">六、實務最佳實踐</h2>\n<h3 id=\"6-1-參數調整技巧\">6.1 參數調整技巧</h3>\n<p><strong>k-NN 距離圖找 eps 值：</strong></p>\n<p>k-distance 圖表是選擇 eps 的標準方法 [來源：DataCamp DBSCAN 指南, 2026]。繪製每個點到第 k 個最近鄰的距離（k = min_samples），並排序。圖中出現明顯彎曲的「膝點」位置即為建議的 eps 值：</p>\n<pre><code class=\"language-python\">from sklearn.neighbors import NearestNeighbors\nimport matplotlib.pyplot as plt\nimport numpy as np\n\n# 計算 k-NN 距離\nk = 5  # 與 min_samples 一致\nneighbors = NearestNeighbors(n_neighbors=k)\nneighbors_fit = neighbors.fit(X_scaled)\ndistances, indices = neighbors_fit.kneighbors(X_scaled)\n\n# 排序第 k 個鄰居的距離\ndistances = np.sort(distances[:, k-1], axis=0)\n\n# 繪製 k-distance 圖\nplt.figure(figsize=(10, 6))\nplt.plot(distances)\nplt.ylabel(f'{k}-NN Distance')\nplt.xlabel('Data Points (sorted by distance)')\nplt.title(f'{k}-Distance Graph for DBSCAN eps Selection')\nplt.grid(True)\n\n# 手動標記膝點（示範）\nknee_point = 150  # 實務上需目視判斷或使用 kneed 套件自動偵測\nplt.axhline(y=distances[knee_point], color='r', linestyle='--',\n            label=f'Suggested eps={distances[knee_point]:.3f}')\nplt.axvline(x=knee_point, color='r', linestyle='--')\nplt.legend()\nplt.show()\n\nprint(f&quot;建議 eps 值: {distances[knee_point]:.3f}&quot;)</code></pre>\n<p><strong>自動膝點偵測（使用 kneed 套件）：</strong></p>\n<pre><code class=\"language-python\">from kneed import KneeLocator\n\nkl = KneeLocator(range(len(distances)), distances, curve='convex', direction='increasing')\nknee_index = kl.knee\n\nif knee_index:\n    suggested_eps = distances[knee_index]\n    print(f&quot;自動偵測建議 eps: {suggested_eps:.3f}&quot;)\nelse:\n    print(&quot;未偵測到明顯膝點，建議手動調整&quot;)</code></pre>\n<p><strong>min_samples 經驗法則：</strong></p>\n<p>根據實務經驗 [來源：Mastering DBSCAN with Scikit-learn, 2024]：</p>\n<ul>\n<li><strong>一般規則</strong>：<code>min_samples ≥ 資料維度 × 2</code></li>\n<li><strong>低維資料（2D-3D）</strong>：min_samples = 4-5 即可</li>\n<li><strong>高維資料（10D+）</strong>：增加至 10-20，避免「維度詛咒」</li>\n<li><strong>噪音較多</strong>：增加 min_samples 提高聚類穩健性</li>\n<li><strong>小型資料集（&lt; 1000 點）</strong>：min_samples = 3-5，避免過度分割</li>\n</ul>\n<p><strong>試驗策略（Grid Search）：</strong></p>\n<pre><code class=\"language-python\">from sklearn.metrics import silhouette_score\n\n# 定義參數範圍\neps_values = np.linspace(0.3, 1.0, 10)\nmin_samples_values = [3, 5, 7, 10]\n\nbest_score = -1\nbest_params = {}\n\nfor eps in eps_values:\n    for min_samples in min_samples_values:\n        dbscan = DBSCAN(eps=eps, min_samples=min_samples)\n        labels = dbscan.fit_predict(X_scaled)\n\n        # 排除噪音點計算分數\n        mask = labels != -1\n        if len(set(labels[mask])) &gt; 1:  # 至少 2 個聚類\n            score = silhouette_score(X_scaled[mask], labels[mask])\n\n            if score &gt; best_score:\n                best_score = score\n                best_params = {'eps': eps, 'min_samples': min_samples}\n\nprint(f&quot;最佳參數: eps={best_params['eps']:.3f}, min_samples={best_params['min_samples']}&quot;)\nprint(f&quot;最佳 Silhouette Score: {best_score:.3f}&quot;)</code></pre>\n<h3 id=\"6-2-dbscan-vs-hdbscan-選擇指南\">6.2 DBSCAN vs HDBSCAN 選擇指南</h3>\n<p>根據資料特性選擇合適演算法 [來源：HDBSCAN vs DBSCAN - Daily Dose of Data Science, 2024]：</p>\n<p><strong>選擇 DBSCAN 的情境：</strong></p>\n<ul>\n<li>資料集具有均勻密度分佈</li>\n<li>需要明確控制聚類密度（透過 eps）</li>\n<li>資料集較小（&lt; 10,000 點），執行速度優先</li>\n<li>需要與其他工具整合（sklearn 生態系完全相容）</li>\n</ul>\n<p><strong>選擇 HDBSCAN 的情境：</strong></p>\n<ul>\n<li>資料集具有變化的密度分佈</li>\n<li>不確定合適的 eps 值，希望演算法自動決定</li>\n<li>需要預測新資料點的聚類標籤</li>\n<li>需要 soft clustering（成員分數）</li>\n<li>需要視覺化聚類階層結構（condensed tree）</li>\n</ul>\n<p><strong>效能比較：</strong></p>\n<table>\n<tr><th>特性</th><th>DBSCAN</th><th>HDBSCAN</th></tr>\n<tr><td>時間複雜度</td><td>O(n log n)</td><td>O(n log n)</td></tr>\n<tr><td>空間複雜度</td><td>O(n)</td><td>O(n²) (condensed tree)</td></tr>\n<tr><td>參數數量</td><td>2（eps, min_samples）</td><td>1（min_cluster_size）</td></tr>\n<tr><td>處理變化密度</td><td>❌</td><td>✅</td></tr>\n<tr><td>預測新點</td><td>❌</td><td>✅</td></tr>\n<tr><td>階層視覺化</td><td>❌</td><td>✅</td></tr>\n</table>\n<h3 id=\"6-3-常見陷阱\">6.3 常見陷阱</h3>\n<p><strong>陷阱 1：未縮放特徵</strong></p>\n<p>DBSCAN 依賴距離計算，不同量級的特徵會導致某些維度主導聚類結果 [來源：scikit-learn DBSCAN 文件, 2024]。</p>\n<pre><code class=\"language-python\"># ❌ 錯誤示範：直接使用原始資料\ndbscan = DBSCAN(eps=0.5, min_samples=5)\nlabels = dbscan.fit_predict(X)  # X 未縮放\n\n# ✅ 正確做法：特徵縮放\nfrom sklearn.preprocessing import StandardScaler\nscaler = StandardScaler()\nX_scaled = scaler.fit_transform(X)\nlabels = dbscan.fit_predict(X_scaled)</code></pre>\n<p><strong>陷阱 2：參數選擇不當</strong></p>\n<p>過小的 eps 或過大的 min_samples 會將大部分點標記為噪音：</p>\n<pre><code class=\"language-python\"># 檢查噪音比例\nn_noise = list(labels).count(-1)\nnoise_ratio = n_noise / len(labels)\n\nif noise_ratio &gt; 0.3:\n    print(f&quot;警告：噪音比例過高 ({noise_ratio:.1%})，建議調整參數&quot;)\n    print(&quot;建議：增加 eps 或減少 min_samples&quot;)</code></pre>\n<p><strong>陷阱 3：忽略噪音點</strong></p>\n<p>在評估聚類品質時，若不排除噪音點會導致錯誤的分數計算：</p>\n<pre><code class=\"language-python\"># ❌ 錯誤：包含噪音點\nscore = silhouette_score(X_scaled, labels)\n\n# ✅ 正確：排除噪音點\nmask = labels != -1\nif len(set(labels[mask])) &gt; 1:\n    score = silhouette_score(X_scaled[mask], labels[mask])</code></pre>\n<p><strong>陷阱 4：高維資料的「維度詛咒」</strong></p>\n<p>在高維空間中，所有點之間的距離趨於相似，DBSCAN 效能下降。建議使用降維技術（PCA、t-SNE）：</p>\n<pre><code class=\"language-python\">from sklearn.decomposition import PCA\n\n# 降維至 10 個主成分\npca = PCA(n_components=10)\nX_reduced = pca.fit_transform(X_scaled)\n\n# 在降維後的資料執行 DBSCAN\ndbscan = DBSCAN(eps=0.5, min_samples=5)\nlabels = dbscan.fit_predict(X_reduced)</code></pre>\n<p><strong>陷阱 5：資料不平衡</strong></p>\n<p>若資料集包含大小差異極大的聚類，單一 eps 值可能無法適應所有聚類。此情境建議使用 HDBSCAN：</p>\n<pre><code class=\"language-python\"># 資料不平衡時優先使用 HDBSCAN\nclusterer = hdbscan.HDBSCAN(min_cluster_size=5)\nclusterer.fit(X_scaled)\nlabels = clusterer.labels_</code></pre>\n<hr>\n<h2 id=\"七-完整程式碼範例-端到端\">七、完整程式碼範例（端到端）</h2>\n<p>以下是從資料載入到結果評估的完整流程，整合所有最佳實踐：</p>\n<pre><code class=\"language-python\">&quot;&quot;&quot;\nDBSCAN/HDBSCAN 完整實作流程\n功能：載入資料、前處理、參數選擇、聚類、評估、視覺化\n&quot;&quot;&quot;\n\nimport numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nimport seaborn as sns\nfrom sklearn.datasets import load_iris\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.cluster import DBSCAN\nfrom sklearn.metrics import silhouette_score\nfrom sklearn.neighbors import NearestNeighbors\nimport hdbscan\n\n# ========================================\n# 1. 資料載入與探索\n# ========================================\nprint(&quot;Step 1: 載入資料&quot;)\niris = load_iris()\nX = iris.data\ny_true = iris.target\nfeature_names = iris.feature_names\n\nprint(f&quot;資料形狀: {X.shape}&quot;)\nprint(f&quot;特徵名稱: {feature_names}&quot;)\n\n# 建立 DataFrame 便於分析\ndf = pd.DataFrame(X, columns=feature_names)\nprint(&quot;\\n資料統計摘要:&quot;)\nprint(df.describe())\n\n# ========================================\n# 2. 特徵縮放\n# ========================================\nprint(&quot;\\nStep 2: 特徵縮放&quot;)\nscaler = StandardScaler()\nX_scaled = scaler.fit_transform(X)\n\nprint(f&quot;縮放前範圍: {X.min(axis=0)} ~ {X.max(axis=0)}&quot;)\nprint(f&quot;縮放後範圍: {X_scaled.min(axis=0)} ~ {X_scaled.max(axis=0)}&quot;)\nprint(f&quot;縮放後均值: {X_scaled.mean(axis=0)}&quot;)\nprint(f&quot;縮放後標準差: {X_scaled.std(axis=0)}&quot;)\n\n# ========================================\n# 3. DBSCAN 參數選擇（k-distance 圖）\n# ========================================\nprint(&quot;\\nStep 3: DBSCAN 參數選擇&quot;)\nk = 4  # min_samples 候選值\nneighbors = NearestNeighbors(n_neighbors=k)\nneighbors_fit = neighbors.fit(X_scaled)\ndistances, indices = neighbors_fit.kneighbors(X_scaled)\n\n# 排序距離\ndistances_sorted = np.sort(distances[:, k-1], axis=0)\n\n# 繪製 k-distance 圖\nplt.figure(figsize=(10, 6))\nplt.plot(distances_sorted)\nplt.ylabel(f'{k}-NN Distance')\nplt.xlabel('Data Points (sorted)')\nplt.title('k-Distance Graph for eps Selection')\nplt.grid(True)\n\n# 目視判斷膝點（示範用）\nknee_point = 50\nsuggested_eps = distances_sorted[knee_point]\nplt.axhline(y=suggested_eps, color='r', linestyle='--',\n            label=f'Suggested eps={suggested_eps:.3f}')\nplt.legend()\nplt.savefig('k_distance_graph.png', dpi=150, bbox_inches='tight')\nplt.show()\n\nprint(f&quot;建議 eps 值: {suggested_eps:.3f}&quot;)\n\n# ========================================\n# 4. 執行 DBSCAN\n# ========================================\nprint(&quot;\\nStep 4: 執行 DBSCAN&quot;)\ndbscan = DBSCAN(eps=0.5, min_samples=4)\ndbscan_labels = dbscan.fit_predict(X_scaled)\n\nn_clusters_dbscan = len(set(dbscan_labels)) - (1 if -1 in dbscan_labels else 0)\nn_noise_dbscan = list(dbscan_labels).count(-1)\n\nprint(f&quot;DBSCAN 聚類數量: {n_clusters_dbscan}&quot;)\nprint(f&quot;DBSCAN 噪音點數量: {n_noise_dbscan}&quot;)\nprint(f&quot;DBSCAN 核心樣本數量: {len(dbscan.core_sample_indices_)}&quot;)\n\n# ========================================\n# 5. 執行 HDBSCAN\n# ========================================\nprint(&quot;\\nStep 5: 執行 HDBSCAN&quot;)\nhdbscan_clusterer = hdbscan.HDBSCAN(min_cluster_size=5, min_samples=3)\nhdbscan_clusterer.fit(X_scaled)\nhdbscan_labels = hdbscan_clusterer.labels_\nhdbscan_probs = hdbscan_clusterer.probabilities_\n\nn_clusters_hdbscan = len(set(hdbscan_labels)) - (1 if -1 in hdbscan_labels else 0)\nn_noise_hdbscan = list(hdbscan_labels).count(-1)\n\nprint(f&quot;HDBSCAN 聚類數量: {n_clusters_hdbscan}&quot;)\nprint(f&quot;HDBSCAN 噪音點數量: {n_noise_hdbscan}&quot;)\nprint(f&quot;HDBSCAN 平均成員分數: {hdbscan_probs.mean():.3f}&quot;)\n\n# ========================================\n# 6. 聚類評估\n# ========================================\nprint(&quot;\\nStep 6: 聚類評估&quot;)\n\n# DBSCAN Silhouette Score\nmask_dbscan = dbscan_labels != -1\nif n_clusters_dbscan &gt; 1:\n    dbscan_score = silhouette_score(X_scaled[mask_dbscan], dbscan_labels[mask_dbscan])\n    print(f&quot;DBSCAN Silhouette Score: {dbscan_score:.3f}&quot;)\nelse:\n    print(&quot;DBSCAN: 聚類數量不足，無法計算 Silhouette Score&quot;)\n\n# HDBSCAN Silhouette Score\nmask_hdbscan = hdbscan_labels != -1\nif n_clusters_hdbscan &gt; 1:\n    hdbscan_score = silhouette_score(X_scaled[mask_hdbscan], hdbscan_labels[mask_hdbscan])\n    print(f&quot;HDBSCAN Silhouette Score: {hdbscan_score:.3f}&quot;)\nelse:\n    print(&quot;HDBSCAN: 聚類數量不足，無法計算 Silhouette Score&quot;)\n\n# ========================================\n# 7. 視覺化比較\n# ========================================\nprint(&quot;\\nStep 7: 視覺化結果&quot;)\n\nfig = plt.figure(figsize=(18, 12))\n\n# 子圖 1：DBSCAN 結果（特徵 0 vs 1）\nax1 = fig.add_subplot(231)\nunique_labels = set(dbscan_labels)\ncolors = [plt.cm.Spectral(each) for each in np.linspace(0, 1, len(unique_labels))]\nfor k, col in zip(unique_labels, colors):\n    if k == -1:\n        col = [0, 0, 0, 1]\n    class_member_mask = (dbscan_labels == k)\n    xy = X_scaled[class_member_mask]\n    ax1.plot(xy[:, 0], xy[:, 1], 'o', markerfacecolor=tuple(col),\n             markeredgecolor='k', markersize=6)\nax1.set_title(f'DBSCAN (eps={dbscan.eps}, min_samples={dbscan.min_samples})')\nax1.set_xlabel('Sepal Length (scaled)')\nax1.set_ylabel('Sepal Width (scaled)')\n\n# 子圖 2：HDBSCAN 結果（特徵 0 vs 1）\nax2 = fig.add_subplot(232)\nscatter = ax2.scatter(X_scaled[:, 0], X_scaled[:, 1], c=hdbscan_labels,\n                      cmap='viridis', s=50, edgecolors='k', linewidths=0.5)\nplt.colorbar(scatter, ax=ax2, label='Cluster Label')\nax2.set_title(f'HDBSCAN (min_cluster_size={hdbscan_clusterer.min_cluster_size})')\nax2.set_xlabel('Sepal Length (scaled)')\nax2.set_ylabel('Sepal Width (scaled)')\n\n# 子圖 3：真實標籤（對照用）\nax3 = fig.add_subplot(233)\nscatter = ax3.scatter(X_scaled[:, 0], X_scaled[:, 1], c=y_true, cmap='Set1', s=50)\nplt.colorbar(scatter, ax=ax3, label='True Label')\nax3.set_title('Ground Truth Labels')\nax3.set_xlabel('Sepal Length (scaled)')\nax3.set_ylabel('Sepal Width (scaled)')\n\n# 子圖 4：DBSCAN 結果（特徵 2 vs 3）\nax4 = fig.add_subplot(234)\nfor k, col in zip(unique_labels, colors):\n    if k == -1:\n        col = [0, 0, 0, 1]\n    class_member_mask = (dbscan_labels == k)\n    xy = X_scaled[class_member_mask]\n    ax4.plot(xy[:, 2], xy[:, 3], 'o', markerfacecolor=tuple(col),\n             markeredgecolor='k', markersize=6)\nax4.set_title('DBSCAN (Features 2-3)')\nax4.set_xlabel('Petal Length (scaled)')\nax4.set_ylabel('Petal Width (scaled)')\n\n# 子圖 5：HDBSCAN 成員分數\nax5 = fig.add_subplot(235)\nscatter = ax5.scatter(X_scaled[:, 2], X_scaled[:, 3], c=hdbscan_probs,\n                      cmap='plasma', s=50, edgecolors='k', linewidths=0.5)\nplt.colorbar(scatter, ax=ax5, label='Membership Probability')\nax5.set_title('HDBSCAN Membership Scores')\nax5.set_xlabel('Petal Length (scaled)')\nax5.set_ylabel('Petal Width (scaled)')\n\n# 子圖 6：聚類數量比較\nax6 = fig.add_subplot(236)\nalgorithms = ['DBSCAN', 'HDBSCAN', 'Ground Truth']\ncluster_counts = [n_clusters_dbscan, n_clusters_hdbscan, len(set(y_true))]\nnoise_counts = [n_noise_dbscan, n_noise_hdbscan, 0]\n\nx = np.arange(len(algorithms))\nwidth = 0.35\n\nbars1 = ax6.bar(x - width/2, cluster_counts, width, label='Clusters')\nbars2 = ax6.bar(x + width/2, noise_counts, width, label='Noise Points')\n\nax6.set_ylabel('Count')\nax6.set_title('Cluster Count Comparison')\nax6.set_xticks(x)\nax6.set_xticklabels(algorithms)\nax6.legend()\n\nplt.tight_layout()\nplt.savefig('clustering_comparison.png', dpi=150, bbox_inches='tight')\nplt.show()\n\n# ========================================\n# 8. HDBSCAN Condensed Tree 視覺化\n# ========================================\nprint(&quot;\\nStep 8: HDBSCAN Condensed Tree&quot;)\nhdbscan_clusterer.condensed_tree_.plot(select_clusters=True,\n                                        selection_palette=sns.color_palette('Set2'))\nplt.title('HDBSCAN Condensed Tree with Cluster Selection')\nplt.savefig('hdbscan_condensed_tree.png', dpi=150, bbox_inches='tight')\nplt.show()\n\n# ========================================\n# 9. 結果匯出\n# ========================================\nprint(&quot;\\nStep 9: 結果匯出&quot;)\n\nresults_df = pd.DataFrame({\n    'sepal_length': X[:, 0],\n    'sepal_width': X[:, 1],\n    'petal_length': X[:, 2],\n    'petal_width': X[:, 3],\n    'dbscan_cluster': dbscan_labels,\n    'hdbscan_cluster': hdbscan_labels,\n    'hdbscan_probability': hdbscan_probs,\n    'true_label': y_true\n})\n\n# 儲存結果\nresults_df.to_csv('clustering_results.csv', index=False)\nprint(&quot;結果已儲存至 clustering_results.csv&quot;)\n\n# 顯示前 10 筆\nprint(&quot;\\n前 10 筆聚類結果:&quot;)\nprint(results_df.head(10))\n\n# ========================================\n# 10. 摘要報告\n# ========================================\nprint(&quot;\\n&quot; + &quot;=&quot;*60)\nprint(&quot;聚類分析完成摘要&quot;)\nprint(&quot;=&quot;*60)\nprint(f&quot;資料集: Iris (150 樣本, 4 特徵)&quot;)\nprint(f&quot;\\nDBSCAN 結果:&quot;)\nprint(f&quot;  - 聚類數量: {n_clusters_dbscan}&quot;)\nprint(f&quot;  - 噪音點: {n_noise_dbscan}&quot;)\nprint(f&quot;  - Silhouette Score: {dbscan_score:.3f}&quot; if n_clusters_dbscan &gt; 1 else &quot;  - Silhouette Score: N/A&quot;)\nprint(f&quot;\\nHDBSCAN 結果:&quot;)\nprint(f&quot;  - 聚類數量: {n_clusters_hdbscan}&quot;)\nprint(f&quot;  - 噪音點: {n_noise_hdbscan}&quot;)\nprint(f&quot;  - 平均成員分數: {hdbscan_probs.mean():.3f}&quot;)\nprint(f&quot;  - Silhouette Score: {hdbscan_score:.3f}&quot; if n_clusters_hdbscan &gt; 1 else &quot;  - Silhouette Score: N/A&quot;)\nprint(f&quot;\\n真實標籤:&quot;)\nprint(f&quot;  - 類別數量: {len(set(y_true))}&quot;)\nprint(&quot;=&quot;*60)</code></pre>\n<p><strong>執行後輸出檔案：</strong></p>\n<ul>\n<li><code>k_distance_graph.png</code>：k-distance 圖表（用於 eps 選擇）</li>\n<li><code>clustering_comparison.png</code>：DBSCAN vs HDBSCAN 視覺化比較</li>\n<li><code>hdbscan_condensed_tree.png</code>：HDBSCAN 階層結構圖</li>\n<li><code>clustering_results.csv</code>：完整聚類結果資料表</li>\n</ul>\n<p><strong>程式碼設計重點：</strong></p>\n<ol>\n<li><strong>模組化結構</strong>：每個步驟獨立，便於修改與除錯</li>\n<li><strong>完整註解</strong>：說明每個參數與決策邏輯</li>\n<li><strong>視覺化優先</strong>：提供多角度視覺化驗證結果</li>\n<li><strong>結果持久化</strong>：儲存圖表與資料表供後續分析</li>\n<li><strong>錯誤處理</strong>：檢查聚類數量避免評估指標計算錯誤</li>\n</ol>\n<hr>\n<h2 id=\"參考來源\">參考來源</h2>\n<ol>\n<li>scikit-learn Development Team (2024). <em>Demo of DBSCAN clustering algorithm</em>. scikit-learn 1.8.0 documentation. https://scikit-learn.org/stable/auto_examples/cluster/plot_dbscan.html</li>\n</ol>\n<ol>\n<li>scikit-learn Development Team (2024). <em>sklearn.cluster.DBSCAN</em>. scikit-learn 1.8.0 documentation. https://scikit-learn.org/stable/modules/generated/sklearn.cluster.DBSCAN.html</li>\n</ol>\n<ol>\n<li>DataCamp (2026). <em>A Guide to the DBSCAN Clustering Algorithm</em>. DataCamp Tutorial. https://www.datacamp.com/tutorial/dbscan-clustering-algorithm</li>\n</ol>\n<ol>\n<li>HDBSCAN Development Team (2024). <em>Basic Usage of HDBSCAN</em> for Clustering*. hdbscan 0.8.1 documentation. https://hdbscan.readthedocs.io/en/latest/basic_hdbscan.html</li>\n</ol>\n<ol>\n<li>HDBSCAN Development Team (2024). <em>How HDBSCAN Works</em>. hdbscan 0.8.1 documentation. https://hdbscan.readthedocs.io/en/latest/how_hdbscan_works.html</li>\n</ol>\n<ol>\n<li>Renesh Bedre (2024). <em>DBSCAN clustering algorithm in Python (with example dataset)</em>. Renesh Bedre Blog. https://www.reneshbedre.com/blog/dbscan-python.html</li>\n</ol>\n<ol>\n<li>Soumen Atta (2024). <em>DBSCAN Clustering with HDBSCAN: A Python Tutorial with Iris Dataset</em>. Medium. https://soumenatta.medium.com/dbscan-clustering-with-hdbscan-a-python-tutorial-with-iris-dataset-5c8779269e4b</li>\n</ol>\n<ol>\n<li>Daily Dose of Data Science (2024). <em>HDBSCAN vs. DBSCAN</em>. Daily Dose of Data Science Blog. https://blog.dailydoseofds.com/p/hdbscan-vs-dbscan</li>\n</ol>\n<ol>\n<li>Daily Dose of Data Science (2024). <em>Intrinsic Measures for Clustering Evaluation</em>. Daily Dose of Data Science Blog. https://blog.dailydoseofds.com/p/intrinsic-measures-for-clustering</li>\n</ol>\n<ol>\n<li>CodePoint Technologies (2024). <em>Mastering DBSCAN with Scikit-learn: A Practical Guide</em>. CodePoint Tech Blog. https://codepointtech.com/mastering-dbscan-with-scikit-learn-a-practical-guide/</li>\n</ol>\n<ol>\n<li>Arize AI (2024). <em>Understanding HDBSCAN: A Deep Dive into Hierarchical Density-Based Clustering</em>. Arize Blog. https://arize.com/blog-course/understanding-hdbscan-a-deep-dive-into-hierarchical-density-based-clustering/</li>\n</ol>\n<ol>\n<li>ProjectPro (2024). <em>How to do Anomaly Detection using Machine Learning in Python?</em>. ProjectPro Article. https://www.projectpro.io/article/anomaly-detection-using-machine-learning-in-python-with-example/555</li>\n</ol>\n<hr>\n<p><strong>文件版本</strong>: 1.0\n<strong>撰寫日期</strong>: 2026-04-15\n<strong>研究階段</strong>: Application（實作應用）\n<strong>總字數</strong>: 約 8,500 字\n<strong>程式碼範例數量</strong>: 23 個</p>\n","headings":[{"level":2,"text":"執行摘要","slug":"執行摘要"},{"level":2,"text":"一、環境準備與套件安裝","slug":"一-環境準備與套件安裝"},{"level":3,"text":"1.1 必要套件","slug":"1-1-必要套件"},{"level":3,"text":"1.2 匯入與初始化","slug":"1-2-匯入與初始化"},{"level":2,"text":"二、DBSCAN 實作指南","slug":"二-dbscan-實作指南"},{"level":3,"text":"2.1 基本用法","slug":"2-1-基本用法"},{"level":3,"text":"2.2 參數調整","slug":"2-2-參數調整"},{"level":3,"text":"2.3 特徵縮放","slug":"2-3-特徵縮放"},{"level":3,"text":"2.4 Iris 資料集實作範例","slug":"2-4-iris-資料集實作範例"},{"level":2,"text":"三、HDBSCAN 實作指南","slug":"三-hdbscan-實作指南"},{"level":3,"text":"3.1 基本用法","slug":"3-1-基本用法"},{"level":3,"text":"3.2 參數調整","slug":"3-2-參數調整"},{"level":3,"text":"3.3 與 DBSCAN 比較","slug":"3-3-與-dbscan-比較"},{"level":3,"text":"3.4 實作範例","slug":"3-4-實作範例"},{"level":2,"text":"四、聚類結果評估","slug":"四-聚類結果評估"},{"level":3,"text":"4.1 Silhouette Score","slug":"4-1-silhouette-score"},{"level":3,"text":"4.2 DBCV 指標","slug":"4-2-dbcv-指標"},{"level":3,"text":"4.3 視覺化方法","slug":"4-3-視覺化方法"},{"level":2,"text":"五、異常檢測應用","slug":"五-異常檢測應用"},{"level":3,"text":"5.1 原理","slug":"5-1-原理"},{"level":3,"text":"5.2 實際案例","slug":"5-2-實際案例"},{"level":2,"text":"六、實務最佳實踐","slug":"六-實務最佳實踐"},{"level":3,"text":"6.1 參數調整技巧","slug":"6-1-參數調整技巧"},{"level":3,"text":"6.2 DBSCAN vs HDBSCAN 選擇指南","slug":"6-2-dbscan-vs-hdbscan-選擇指南"},{"level":3,"text":"6.3 常見陷阱","slug":"6-3-常見陷阱"},{"level":2,"text":"七、完整程式碼範例（端到端）","slug":"七-完整程式碼範例-端到端"},{"level":2,"text":"參考來源","slug":"參考來源"}],"prev":{"slug":"harness-engineering-在-daily-di-a51e03f4","title":"Harness Engineering 在 daily-digest-prompt 的實作藍圖 — 品質閘門配置、約束驗證自動化與 CI/CD 整合（application 階段）"},"next":{"slug":"daily-digest-prompt-专案优化日报-202-8462dc34","title":"Daily Digest Prompt 专案优化日报 2026-04-15"}}