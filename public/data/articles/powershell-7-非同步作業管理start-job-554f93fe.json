{"id":"554f93fe-63bf-4e9c-a7dd-584ae6d93f57","title":"PowerShell 7 非同步作業管理：Start-Job 殭屍防護、Timeout 守衛與 Process Tree Kill","slug":"powershell-7-非同步作業管理start-job-554f93fe","category":"Claude Code","categorySlug":"claude","tags":["技術研究","PowerShell","daily-digest","非同步作業","殭屍進程","Windows排程"],"updatedAt":"2026-04-20","excerpt":"PowerShell 7 非同步作業管理：StartJob 殭屍防護、Timeout 守衛與 Process Tree Kill 技術概述 PowerShell 7 的非同步作業管理（StartJob / System.Diagnostic","readingMin":5,"isNew":false,"html":"<h1 id=\"powershell-7-非同步作業管理-start-job-殭屍防護-time\">PowerShell 7 非同步作業管理：Start-Job 殭屍防護、Timeout 守衛與 Process Tree Kill</h1>\n<h2 id=\"技術概述\">技術概述</h2>\n<p>PowerShell 7 的非同步作業管理（Start-Job / System.Diagnostics.Process）在 Windows 排程環境中存在三個主要失效模式：殭屍進程殘留、stdout/stderr Pipe 死結、以及 Kill 後子進程未終止。掌握這三類陷阱與對應的防護模式，是建立可靠 Windows 排程系統的基礎工程。</p>\n<h2 id=\"核心概念與原理\">核心概念與原理</h2>\n<h3 id=\"1-uv-孤兒問題-已有-issue-確認\">1. uv 孤兒問題（已有 issue 確認）</h3>\n<p><code>uv.exe</code> 在 Windows 上不使用 Job Object 綁定子進程生命週期（[來源：astral-sh/uv #11817, 2025]），因此：</p>\n<ul>\n<li><code>Kill($uvProcess)</code> → uv.exe 終止，但 <code>python.exe</code> 子進程成為孤兒</li>\n<li>必須使用 <code>$proc.Kill($true)</code> (entireProcessTree) 或 <code>taskkill /F /T /PID</code></li>\n<li>SIGINT/Ctrl+C 傳播也不可靠（[來源：astral-sh/uv #8654, 2024]）</li>\n</ul>\n<p>三角佐證：uv #11817（確認）+ uv #8654（SIGINT 不可靠）+ puppetlabs PR #202（相同根因模式）</p>\n<h3 id=\"2-waitforexit-int-的兩階段完成語義\">2. WaitForExit(int) 的兩階段完成語義</h3>\n<p><code>WaitForExit(int milliseconds)</code> 返回 <code>true</code> 後，非同步輸出 handler 可能尚未清空 [來源：Microsoft Learn Process.WaitForExit, .NET 10]。</p>\n<p>正確模式（需兩步）：</p>\n<pre><code class=\"language-powershell\">$exited = $proc.WaitForExit(60000)  # 帶 timeout 等待進程退出\nif ($exited) {\n    $proc.WaitForExit()  # 無參數版：等 BeginOutputReadLine handler 清空\n}</code></pre>\n<p>若跳過第二步，讀到的 JSON 可能截斷（[來源：PS issue #25262, 2025]）。</p>\n<p>三角佐證：.NET 10 文件（明確說明）+ PS issue #25262（實際截斷案例）+ PSScriptAnalyzer #1297（相同症狀）</p>\n<h3 id=\"3-kill-entireprocesstree-true-的-exited-事\">3. Kill(entireProcessTree:true) 的 Exited 事件 Bug</h3>\n<p><code>Kill(entireProcessTree:$true)</code> 在 Windows 上不觸發 <code>Exited</code> 事件（[來源：dotnet/runtime #63328, 2022]），必須改用輪詢：</p>\n<pre><code class=\"language-powershell\">$proc.Kill($true)\n$proc.WaitForExit(5000) | Out-Null  # 輪詢等待，非事件驅動\n$proc.Close()</code></pre>\n<p>三角佐證：dotnet/runtime #63328（已確認 bug）+ .NET Kill() 文件（說明 Kill 是非同步的）+ phantom-killer（生產環境繞過方案）</p>\n<h3 id=\"4-pipe-死結防護-raymond-chen-定律\">4. Pipe 死結防護（Raymond Chen 定律）</h3>\n<p>OS pipe buffer 預設 ~4KB（Windows）。若父進程在 <code>WaitForExit()</code> 中阻塞而不排空 pipe，當子進程輸出超過 4KB 時雙方死結（[來源：Raymond Chen / The Old New Thing, 2011]）。</p>\n<p>唯一可靠修法：<code>BeginOutputReadLine()</code> + <code>BeginErrorReadLine()</code> 必須在 <code>WaitForExit()</code> 之前呼叫：</p>\n<pre><code class=\"language-powershell\">$proc.BeginOutputReadLine()   # 非同步排空 stdout\n$proc.BeginErrorReadLine()    # 非同步排空 stderr\n$proc.WaitForExit(60000)      # 現在安全等待</code></pre>\n<p>三角佐證：Raymond Chen 2011（根因分析）+ Icinga2 #32（生產修復案例）+ PS issue #25262（現代 PS 7 確認仍有此問題）</p>\n<h3 id=\"5-start-job-清理的必要步驟序列\">5. Start-Job 清理的必要步驟序列</h3>\n<p><code>Stop-Job</code> 發信號但不等待 pwsh.exe 終止（[來源：Microsoft Learn about_Jobs, 2025]）。完整清理序列：</p>\n<pre><code class=\"language-powershell\">Stop-Job $job -ErrorAction SilentlyContinue\nWait-Job $job -Timeout 10 | Out-Null  # 確保 pwsh.exe 已終止\nRemove-Job $job -Force</code></pre>\n<h2 id=\"最佳實踐與陷阱\">最佳實踐與陷阱</h2>\n<h3 id=\"禁止模式\">禁止模式</h3>\n<ul>\n<li><code>&amp; uv run python tools/xxx.py</code> 無 timeout — 會殭屍</li>\n<li><code>Start-Process -Wait</code> — PS #25262 確認可無限 hang</li>\n<li>順序呼叫 <code>ReadToEnd()</code> 兩次（stdout 後 stderr）— Pipe 死結</li>\n<li><code>Kill()</code> 後不呼叫 <code>WaitForExit()</code> — Kill 是非同步的</li>\n<li><code>Stop-Job</code> 後不接 <code>Wait-Job</code> — pwsh.exe 仍在運行</li>\n</ul>\n<h3 id=\"必用模式\">必用模式</h3>\n<ul>\n<li><code>System.Diagnostics.Process</code> + <code>WaitForExit(ms)</code> + <code>Kill(entireProcessTree:$true)</code></li>\n<li><code>BeginOutputReadLine()</code> / <code>BeginErrorReadLine()</code> 在 <code>WaitForExit()</code> 前</li>\n<li>Two-stage timeout：<code>WaitForExit(primary)</code> → kill → <code>WaitForExit(5000)</code> → <code>Close()</code></li>\n<li>每個 PS wrapper 的 <code>finally</code> block 確保 Kill+Close</li>\n</ul>\n<h2 id=\"與本專案的關聯\">與本專案的關聯</h2>\n<p>daily-digest-prompt 的 <code>Invoke-UvRunWithTimeout</code>（run-todoist-agent-team.ps1）實作了此模式的最佳實踐，解決了 2026-04-19 殭屍事件與 2026-04-20 wrapper hang 的根因。TimeoutSec 建議值：YAML 解析 30s、小工具 45-60s、中任務 60s、大型 workflow 900s。</p>\n<h2 id=\"程式碼範例\">程式碼範例</h2>\n<h3 id=\"invoke-uvrunwithtimeout-完整實作\">Invoke-UvRunWithTimeout 完整實作</h3>\n<pre><code class=\"language-powershell\">function Invoke-UvRunWithTimeout {\n    param([string[]]$Arguments, [int]$TimeoutSec = 60, [string]$Label = 'uv-run', [string]$WorkDir = $PSScriptRoot)\n    $psi = [System.Diagnostics.ProcessStartInfo]::new()\n    $psi.FileName = 'uv'; $psi.Arguments = ($Arguments | ForEach-Object { &quot;'$_'&quot; }) -join ' '\n    $psi.WorkingDirectory = $WorkDir; $psi.RedirectStandardOutput = $true\n    $psi.RedirectStandardError = $true; $psi.UseShellExecute = $false; $psi.CreateNoWindow = $true\n    $proc = [System.Diagnostics.Process]::new(); $proc.StartInfo = $psi\n    $stdout = [System.Text.StringBuilder]::new(); $stderr = [System.Text.StringBuilder]::new()\n    $proc.add_OutputDataReceived({ param($s,$e) if ($null -ne $e.Data) { $stdout.AppendLine($e.Data) } })\n    $proc.add_ErrorDataReceived({  param($s,$e) if ($null -ne $e.Data) { $stderr.AppendLine($e.Data) } })\n    $proc.Start() | Out-Null\n    $proc.BeginOutputReadLine(); $proc.BeginErrorReadLine()  # 防 pipe 死結\n    $exited = $proc.WaitForExit($TimeoutSec * 1000)\n    if (-not $exited) {\n        Write-Warning &quot;[$Label] Timeout ${TimeoutSec}s — killing tree&quot;\n        try { $proc.Kill($true) } catch { }\n        $proc.WaitForExit(5000) | Out-Null; $proc.Close(); return $null\n    }\n    $proc.WaitForExit(); $proc.Close()  # 無參數：清空 async handler\n    if ($proc.ExitCode -ne 0) { return $null }\n    try { return $stdout.ToString() | ConvertFrom-Json } catch { return $stdout.ToString() }\n}</code></pre>\n<h2 id=\"完整參考書目\">完整參考書目</h2>\n<ol>\n<li>Microsoft Learn (2026). Start-Job (PowerShell 7.6). https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/start-job?view=powershell-7.6</li>\n<li>Microsoft Learn (2026). Process.WaitForExit (.NET 10). https://learn.microsoft.com/en-us/dotnet/api/system.diagnostics.process.waitforexit?view=net-10.0</li>\n<li>Microsoft Learn (2026). Process.Kill (.NET 10). https://learn.microsoft.com/en-us/dotnet/api/system.diagnostics.process.kill?view=net-10.0</li>\n<li>Microsoft Learn (2025). about_Jobs (PS 7.5). https://learn.microsoft.com/en-us/powershell/module/microsoft.powershell.core/about/about_jobs?view=powershell-7.5</li>\n<li>GitHub/PowerShell (2021). Start-Process -Wait vs Wait-Process child tree — #15555. https://github.com/PowerShell/PowerShell/issues/15555</li>\n<li>GitHub/PowerShell (2023). Exiting pwsh orphans Start-Process children — #21149. https://github.com/PowerShell/PowerShell/issues/21149</li>\n<li>GitHub/PowerShell (2025). Start-Process -Wait infinite hang — #25262. https://github.com/PowerShell/PowerShell/issues/25262</li>\n<li>GitHub/dotnet (2022). Kill(entireProcessTree:true) does not fire Exited event — #63328. https://github.com/dotnet/runtime/issues/63328</li>\n<li>GitHub/astral-sh (2025). uv does not kill spawned processes on Windows — #11817. https://github.com/astral-sh/uv/issues/11817</li>\n<li>GitHub/astral-sh (2024). uv run SIGINT propagation unreliable on Windows — #8654. https://github.com/astral-sh/uv/issues/8654</li>\n<li>GitHub/puppetlabs (2018). Prevent zombie procs from timeout — puppetlabs-powershell PR #202. https://github.com/puppetlabs/puppetlabs-powershell/pull/202</li>\n<li>GitHub/Icinga2 (2020). Fix deadlock on processStart — #32. https://github.com/Icinga/icinga2-powershell-module/issues/32</li>\n<li>Raymond Chen / Microsoft DevBlogs (2011). Be careful when redirecting both stdin and stdout to pipes. https://devblogs.microsoft.com/oldnewthing/20110707-00/?p=10223</li>\n<li>Alkane Solutions (2023). Setting a Timeout in PowerShell Scripts to Prevent Hanging. https://www.alkanesolutions.co.uk/2023/12/22/setting-a-timeout-in-powershell-scripts-to-prevent-hanging/</li>\n<li>GitHub/chris-sekira (2020). phantom-killer — PowerShell zombie process scanner. https://github.com/chris-sekira/phantom-killer</li>\n</ol>\n","headings":[{"level":2,"text":"技術概述","slug":"技術概述"},{"level":2,"text":"核心概念與原理","slug":"核心概念與原理"},{"level":3,"text":"1. uv 孤兒問題（已有 issue 確認）","slug":"1-uv-孤兒問題-已有-issue-確認"},{"level":3,"text":"2. WaitForExit(int) 的兩階段完成語義","slug":"2-waitforexit-int-的兩階段完成語義"},{"level":3,"text":"3. Kill(entireProcessTree:true) 的 Exited 事件 Bug","slug":"3-kill-entireprocesstree-true-的-exited-事"},{"level":3,"text":"4. Pipe 死結防護（Raymond Chen 定律）","slug":"4-pipe-死結防護-raymond-chen-定律"},{"level":3,"text":"5. Start-Job 清理的必要步驟序列","slug":"5-start-job-清理的必要步驟序列"},{"level":2,"text":"最佳實踐與陷阱","slug":"最佳實踐與陷阱"},{"level":3,"text":"禁止模式","slug":"禁止模式"},{"level":3,"text":"必用模式","slug":"必用模式"},{"level":2,"text":"與本專案的關聯","slug":"與本專案的關聯"},{"level":2,"text":"程式碼範例","slug":"程式碼範例"},{"level":3,"text":"Invoke-UvRunWithTimeout 完整實作","slug":"invoke-uvrunwithtimeout-完整實作"},{"level":2,"text":"完整參考書目","slug":"完整參考書目"}],"prev":{"slug":"專案rag優化日報-2026-04-21-91f0afc3","title":"專案RAG優化日報 2026-04-21"},"next":{"slug":"rag-文件匯入品質治理閱讀順序表格保真ocr-與-c-5b4fd3cc","title":"RAG 文件匯入品質治理：閱讀順序、表格保真、OCR 與 chunking-ready Markdown 的工程選型"}}