{"id":"c91ad542-8623-4dfe-a753-fc215ce42ba9","title":"Python httpx 非同步 HTTP 客戶端生產實踐 2026：連接池管理、重試策略、逾時配置與 AI Agent 工作流 API 可靠度設計","slug":"python-httpx-非同步-http-客戶端生產實踐--c91ad542","category":"Claude Code","categorySlug":"claude","tags":["技術研究","httpx","Python","async","HTTP","連接池","重試策略","AI-Agent","daily-digest"],"updatedAt":"2026-04-30","excerpt":"Python httpx 非同步 HTTP 客戶端生產實踐 2026 研究日期：20260430 作者：dailydigest tech_research 適用版本：httpx 0.27+（202604） 技術概述 httpx 是基於 ht","readingMin":10,"isNew":false,"html":"<h1 id=\"python-httpx-非同步-http-客戶端生產實踐-2026\">Python httpx 非同步 HTTP 客戶端生產實踐 2026</h1>\n<p><strong>研究日期</strong>：2026-04-30  \n<strong>作者</strong>：daily-digest tech_research  \n<strong>適用版本</strong>：httpx 0.27+（2026-04）  </p>\n<hr>\n<h2 id=\"技術概述\">技術概述</h2>\n<p>httpx 是基於 <code>httpcore</code> 傳輸層的現代 Python HTTP 客戶端，同時支援同步（<code>Client</code>）與非同步（<code>AsyncClient</code>）操作介面，從根本上解決了 <code>requests</code> 只能同步、<code>aiohttp</code> 只能非同步的分裂問題。其主要技術特性：</p>\n<ul>\n<li><strong>完整 HTTP/2 支援</strong>（multiplexing + header compression），需 <code>pip install 'httpx[http2]'</code></li>\n<li><strong>預設 5 秒超時</strong>（安全預設，相較於 <code>requests</code> 的無限等待）</li>\n<li><strong>完整型別標註</strong>（Type hints，IDE 友好）</li>\n<li><strong>相容三種非同步後端</strong>：asyncio、trio、anyio</li>\n<li><strong>requests-compatible API</strong>，遷移成本低</li>\n</ul>\n<p>適用場景：FastAPI/ASGI 框架的 API 呼叫、AI Agent 工具函式（tool calls）、多 API 並行批次請求、爬蟲。</p>\n<p>與主要替代方案的差異：</p>\n<table>\n<tr><th>特性</th><th>httpx</th><th>requests</th><th>aiohttp</th></tr>\n<tr><td>Native async</td><td>✅</td><td>❌</td><td>✅</td></tr>\n<tr><td>HTTP/2</td><td>✅</td><td>❌</td><td>❌</td></tr>\n<tr><td>同步介面</td><td>✅</td><td>✅</td><td>❌</td></tr>\n<tr><td>超高並發（&gt;300）效能</td><td>⚠️</td><td>N/A</td><td>✅</td></tr>\n<tr><td>與 requests API 相容</td><td>✅</td><td>—</td><td>❌</td></tr>\n</table>\n<p>[來源：fooish.com Python httpx 教學, 2026; Oxylabs HTTPX vs Requests vs AIOHTTP 比較]</p>\n<hr>\n<h2 id=\"核心概念與原理\">核心概念與原理</h2>\n<h3 id=\"1-傳輸架構-transport-layer\">1. 傳輸架構（Transport Layer）</h3>\n<p>httpx 架構分三層：</p>\n<ol>\n<li><code>httpx.AsyncClient</code> / <code>httpx.Client</code> — 高層公共 API，處理重定向、Cookie、認證</li>\n<li><code>httpcore</code>（底層傳輸）— 管理 HTTP/1.1 連接池和 HTTP/2 多路複用</li>\n<li><code>asyncio</code> / <code>trio</code> socket — 實際網路 IO</li>\n</ol>\n<p><code>AsyncHTTPTransport</code> 是預設非同步傳輸實作，<code>HTTPTransport</code> 是同步版本。兩者均支援 <code>retries</code> 參數進行底層<strong>網路層級</strong>重試（連接失敗、逾時），但不處理 HTTP 狀態碼（如 429）的應用層重試。</p>\n<p>[來源：Python httpx 官方文件 Transports, 2026]</p>\n<h3 id=\"2-連接池管理-connection-pooling\">2. 連接池管理（Connection Pooling）</h3>\n<p>httpx 透過 <code>httpx.Limits</code> 控制三個連接池參數：</p>\n<ul>\n<li><strong>`max_keepalive_connections=20`</strong>：允許的 keep-alive 連接數（default 20）</li>\n<li><strong>`max_connections=100`</strong>：最大總連接數，超過則進入等待佇列（default 100）</li>\n<li><strong>`keepalive_expiry=5`</strong>：空閒 keep-alive 連接的存活秒數（default 5s）</li>\n</ul>\n<p>連接池的設計目標是重用 TCP 連接，避免每次請求的 TCP handshake 和 TLS negotiation 開銷。<code>PoolTimeout</code>（從 <code>httpx.Timeout</code> 的 pool 參數設定）在無法及時從池取得連接時拋出。</p>\n<p><strong>核心原則</strong>：<em>不要在熱路徑（hot loop）內建立 Client</em>。每次 <code>async with httpx.AsyncClient()</code> 都會建立全新連接池，無法重用舊連接，連接建立成本高昂。</p>\n<p>[來源：Python httpx 官方文件 Clients — &quot;Creating an AsyncClient is expensive&quot;, 2026]</p>\n<h3 id=\"3-四維超時系統-timeout-system\">3. 四維超時系統（Timeout System）</h3>\n<p>httpx 的超時分成四個獨立階段，每個階段都可獨立配置：</p>\n<table>\n<tr><th>超時類型</th><th>說明</th><th>例外類型</th></tr>\n<tr><td><strong>Connect</strong></td><td>建立 socket 連接的等待時間</td><td><code>ConnectTimeout</code></td></tr>\n<tr><td><strong>Read</strong></td><td>等待回應資料塊的間隔時間</td><td><code>ReadTimeout</code></td></tr>\n<tr><td><strong>Write</strong></td><td>發送請求資料塊的等待時間</td><td><code>WriteTimeout</code></td></tr>\n<tr><td><strong>Pool</strong></td><td>從連接池取得連接的等待時間</td><td><code>PoolTimeout</code></td></tr>\n</table>\n<p><strong>預設行為</strong>：所有階段共用 5 秒總逾時（<code>httpx.Timeout(5.0)</code>）。</p>\n<p>細粒度設定方式：</p>\n<pre><code class=\"language-python\"># 不同階段設定不同超時\ntimeout = httpx.Timeout(30.0, connect=5.0, pool=10.0)\n# 30s read/write，5s connect，10s pool 等待\nclient = httpx.AsyncClient(timeout=timeout)</code></pre>\n<p>注意：<code>httpx.TimeoutException</code> 是所有超時例外的父類，可一次捕捉所有類型。</p>\n<p>[來源：Python httpx 官方文件 Timeouts — &quot;HTTPX is careful to enforce timeouts everywhere by default&quot;, 2026]</p>\n<h3 id=\"4-asyncclient-生命週期管理\">4. AsyncClient 生命週期管理</h3>\n<p>兩種管理方式：</p>\n<pre><code class=\"language-python\"># ✅ 推薦：context manager，自動 aclose()\nasync with httpx.AsyncClient() as client:\n    response = await client.get(url)\n\n# ⚠️ 需手動管理：開發者負責 aclose()\nclient = httpx.AsyncClient()\ntry:\n    response = await client.get(url)\nfinally:\n    await client.aclose()  # 不可省略！</code></pre>\n<p>在 Streaming 手動模式下，<code>Response.aclose()</code> 同樣必須呼叫，否則 TCP 連接留置不釋放。</p>\n<p>[來源：Python httpx 官方文件 Async Support, 2026; GitHub Issues #1155 e2b-dev/E2B]</p>\n<h3 id=\"5-http-2-多路複用\">5. HTTP/2 多路複用</h3>\n<p>啟用 <code>http2=True</code> 後，httpx 可在單一 TCP 連接上同時發起多個請求（stream multiplexing + header compression）：</p>\n<ul>\n<li>需要伺服器支援 ALPN 協商，否則自動 fallback 到 HTTP/1.1</li>\n<li>基準測試：HTTP/2 模式比 HTTP/1.1 快 ~17%（1000 requests benchmark: 8.5s vs 10.22s）</li>\n<li>但 aiohttp（HTTP/1.1）在高並發場景仍快 httpx HTTP/2 兩倍以上（針對非 HTTP/2 服務器）</li>\n</ul>\n<p>[來源：Oxylabs HTTPX vs Requests vs AIOHTTP 比較, 2025]</p>\n<hr>\n<h2 id=\"最佳實踐\">最佳實踐</h2>\n<h3 id=\"1-應用層級單一客戶端-single-client-instance\">1. 應用層級單一客戶端（Single Client Instance）</h3>\n<p><strong>情境</strong>：長時間運行的 AI Agent 服務或 FastAPI 應用  \n<strong>建議</strong>：在 lifespan 事件或模組層級建立單一 AsyncClient</p>\n<pre><code class=\"language-python\"># FastAPI lifespan 最佳實踐\nfrom contextlib import asynccontextmanager\nimport httpx\nfrom fastapi import FastAPI\n\n@asynccontextmanager\nasync def lifespan(app: FastAPI):\n    # 啟動：建立共享客戶端\n    app.state.http_client = httpx.AsyncClient(\n        limits=httpx.Limits(\n            max_keepalive_connections=20,\n            max_connections=100,\n            keepalive_expiry=30,\n        ),\n        timeout=httpx.Timeout(30.0, connect=10.0),\n    )\n    yield\n    # 關閉：正確釋放連接\n    await app.state.http_client.aclose()\n\napp = FastAPI(lifespan=lifespan)</code></pre>\n<p><strong>依據</strong>：官方文件明確指出「Creating an AsyncClient is expensive」，並推薦 lifespan 模式。\n<strong>反例</strong>：在 request handler 內 <code>async with httpx.AsyncClient()</code>，每次請求重建連接池。</p>\n<p>[來源：Python httpx 官方文件 Clients; BetterStack httpx guide, 2026]</p>\n<h3 id=\"2-結構化超時配置策略\">2. 結構化超時配置策略</h3>\n<p>依 API 類型選用不同超時預算：</p>\n<table>\n<tr><th>API 類型</th><th>connect</th><th>read</th><th>備註</th></tr>\n<tr><td>本地 KB API</td><td>3s</td><td>30s</td><td>localhost，TCP 連接快</td></tr>\n<tr><td>外部 LLM API（如 Claude）</td><td>5s</td><td>120s</td><td>考慮 cold start</td></tr>\n<tr><td>ntfy 通知</td><td>5s</td><td>10s</td><td>快速失敗為佳</td></tr>\n<tr><td>爬蟲/外部站點</td><td>5s</td><td>30s</td><td>一般建議</td></tr>\n</table>\n<p><strong>絕對禁止 `timeout=None`</strong>：在 AI Agent 工作流中，上游服務偶爾無回應，無限等待會讓整個 Phase 掛起，造成 Task Scheduler 0x41301 殭屍狀態。</p>\n<p>[來源：dasroot.net 2026-02; Python httpx 官方文件 Timeouts]</p>\n<h3 id=\"3-httpx-retries-聲明式重試-網路層\">3. httpx-retries 聲明式重試（網路層）</h3>\n<p><code>httpx-retries</code> 透過 <code>RetryTransport</code> 插入重試邏輯，處理連接失敗等網路層錯誤：</p>\n<pre><code class=\"language-python\">from httpx_retries import Retry, RetryTransport\n\nretry = Retry(total=3, backoff_factor=0.5)\n# total=3：最多重試 3 次\n# backoff_factor=0.5：{0.5s, 1.0s, 2.0s} 退避延遲\n\nasync with httpx.AsyncClient(\n    transport=RetryTransport(retry=retry)\n) as client:\n    response = await client.get(&quot;https://api.example.com/data&quot;)</code></pre>\n<p><strong>依據</strong>：網路層重試（<code>ConnectError</code>、<code>ConnectTimeout</code>）與應用層重試（HTTP 429、503）分開處理，職責明確。</p>\n<p>[來源：httpx-retries 官方文件, will-ockmore.github.io]</p>\n<h3 id=\"4-tenacity-httpx-處理-http-429-rate-limit\">4. tenacity + httpx 處理 HTTP 429 Rate Limit</h3>\n<p>httpx-retries 不處理 HTTP 狀態碼，需用 tenacity 在應用層處理：</p>\n<pre><code class=\"language-python\">import asyncio\nimport random\nimport httpx\nfrom tenacity import (\n    retry, stop_after_attempt,\n    wait_exponential, retry_if_exception_type\n)\n\n@retry(\n    stop=stop_after_attempt(5),\n    wait=wait_exponential(multiplier=0.5, min=1, max=30),\n    retry=retry_if_exception_type(httpx.HTTPStatusError),\n)\nasync def api_call_with_retry(\n    client: httpx.AsyncClient, url: str\n) -&gt; dict:\n    response = await client.get(url)\n    if response.status_code == 429:\n        # 優先讀取 Retry-After header\n        retry_after = float(response.headers.get(&quot;Retry-After&quot;, 1.0))\n        jitter = random.uniform(0, 0.5)\n        await asyncio.sleep(retry_after + jitter)\n        response.raise_for_status()  # 觸發 tenacity retry\n    response.raise_for_status()\n    return response.json()</code></pre>\n<p>重試策略原則（2026 生產建議）：</p>\n<ul>\n<li>重試條件：HTTP 429、500、502、503、504 + <code>ConnectError</code> + <code>TimeoutException</code></li>\n<li><strong>不重試</strong>：401、403、400（客戶端錯誤）</li>\n<li>Base delay: 0.5s，指數係數: 2.0，最大: 30s</li>\n<li>加 jitter（全隨機）防止 retry storm</li>\n<li>優先讀取 <code>Retry-After</code> header，勿用固定延遲</li>\n</ul>\n<p>[來源：Fastio AI Agent Retry Patterns 2026; dasroot.net 2026-02]</p>\n<h3 id=\"5-asyncio-gather-並行請求模式\">5. asyncio.gather 並行請求模式</h3>\n<pre><code class=\"language-python\">async def fetch_all(urls: list[str]) -&gt; list[dict]:\n    async with httpx.AsyncClient(\n        limits=httpx.Limits(max_connections=50, max_keepalive_connections=20)\n    ) as client:\n        tasks = [\n            client.get(url, timeout=httpx.Timeout(30.0, connect=5.0))\n            for url in urls\n        ]\n        responses = await asyncio.gather(*tasks, return_exceptions=True)\n    # 過濾例外\n    return [\n        r.json() for r in responses\n        if not isinstance(r, Exception)\n    ]</code></pre>\n<p><strong>效能收益</strong>：5 個各需 2 秒的請求，並行執行總耗時 ~2 秒 vs 循序 10 秒（5x 加速）。</p>\n<p>[來源：dailypypy.org 正體中文 httpx 教學; oneuptime.com 2026-02]</p>\n<hr>\n<h2 id=\"陷阱與反例\">陷阱與反例</h2>\n<h3 id=\"陷阱一-熱路徑建立多個客戶端-p0-最常見\">陷阱一：熱路徑建立多個客戶端（P0 — 最常見）</h3>\n<pre><code class=\"language-python\"># ❌ 反例：每次呼叫重建連接池\nasync def bad_approach(url: str) -&gt; dict:\n    async with httpx.AsyncClient() as client:  # 每次都是全新連接池\n        return (await client.get(url)).json()\n\n# ✅ 正解：Client 提升到模組層級或 lifespan\n_shared_client: httpx.AsyncClient | None = None\n\ndef get_client() -&gt; httpx.AsyncClient:\n    global _shared_client\n    if _shared_client is None:\n        _shared_client = httpx.AsyncClient(\n            timeout=httpx.Timeout(30.0, connect=5.0),\n        )\n    return _shared_client</code></pre>\n<p><strong>成因</strong>：<code>AsyncClient</code> 建立成本高（初始化 httpcore ConnectionPool、SSL context 驗證、後端事件循環綁定）。在高頻路徑重複建立完全抵銷連接複用效能收益，且可能造成 fd 洩漏。</p>\n<p>[來源：Python httpx 官方文件 Clients — &quot;Creating an AsyncClient is expensive&quot;; GitHub Discussion #2444]</p>\n<h3 id=\"陷阱二-忘記-aclose-造成資源洩漏\">陷阱二：忘記 aclose() 造成資源洩漏</h3>\n<p><strong>失敗模式</strong>：手動建立 <code>AsyncClient</code>（不用 <code>async with</code>）但沒有在 exception path 呼叫 <code>aclose()</code>，導致 TCP 連接和 fd 洩漏。</p>\n<p><strong>真實案例</strong>：GitHub Issue #1155 (e2b-dev/E2B) — httpx.AsyncClient 在 sandbox 強制終止時未關閉，生產環境出現連接池洩漏，需要定期重啟服務修復。</p>\n<p><strong>規避方法</strong>：永遠使用 <code>async with</code>，或確保 <code>finally</code> 區塊中呼叫 <code>aclose()</code>。</p>\n<p>[來源：GitHub Issues #1155 e2b-dev/E2B, 2024; httpx 官方文件 Async Support]</p>\n<h3 id=\"陷阱三-超高並發場景-readerror-gt-300-concurrent\">陷阱三：超高並發場景 ReadError（&gt;300 concurrent）</h3>\n<p>在 300 concurrent / 1000 total requests 的壓力測試中，httpx 出現隨機 <code>httpx.ReadError</code>，且只在高負載下非確定性地觸發，難以重現和除錯。aiohttp 在相同場景下無此問題。</p>\n<p><strong>根因</strong>：httpx 的 <code>AsyncHTTPTransport</code> 使用 httpcore 的連接池，在極高並發下連接競爭（pool contention）超過 <code>max_connections</code> 上限，請求被迫等待並最終 ReadError。</p>\n<p><strong>規避策略</strong>（按優先級）：</p>\n<ol>\n<li>降低並發數：<code>asyncio.Semaphore(50)</code> 限制最大並行請求數</li>\n<li>調高 <code>max_connections</code>：<code>httpx.Limits(max_connections=200, max_keepalive_connections=50)</code></li>\n<li>超高並發（&gt;300）場景改用 aiohttp + <code>TCPConnector(limit=N)</code></li>\n</ol>\n<p>[來源：miguel-mendez-ai.com 高並發測試報告, 2024; GitHub Issue #838 encode/httpx — &quot;HTTPX AsyncClient slower than aiohttp?&quot;]</p>\n<h3 id=\"陷阱四-timeout-none-導致-ai-agent-無限掛起\">陷阱四：timeout=None 導致 AI Agent 無限掛起</h3>\n<pre><code class=\"language-python\"># ❌ 危險：無超時限制\nresponse = await client.get(llm_api_url, timeout=None)</code></pre>\n<p><strong>失敗場景</strong>：LLM API 上游服務偶發無回應，<code>timeout=None</code> 讓整個 Agent Phase 永久掛起，造成 Windows Task Scheduler 進入 0x41301「Running」殭屍狀態，後續排程被阻塞。</p>\n<p><strong>規範</strong>：所有外部 API 呼叫均設置明確超時值，絕不使用 <code>timeout=None</code>。</p>\n<p>[來源：Python httpx 官方文件 Timeouts; 本專案 zombie-lock-subprocess-hang 事故記錄]</p>\n<hr>\n<h2 id=\"與本專案關聯\">與本專案關聯</h2>\n<p>本專案 <code>tools/*.py</code> 目前大量使用 Bash <code>curl</code> 發送 HTTP 請求（KB API、ntfy、Todoist API），存在以下系統性問題：</p>\n<ol>\n<li>每次 <code>curl</code> 都啟動新進程（~10-50ms overhead）、無連接複用</li>\n<li>重試邏輯散落各處，無統一策略</li>\n<li>超時設定不一致（部分用 <code>--max-time</code>，部分未設）</li>\n<li>錯誤處理依賴 exit code 而非結構化例外</li>\n</ol>\n<p><strong>落地場景一：KB API 呼叫統一化</strong></p>\n<p>建立 <code>tools/http_client.py</code> 工廠函式，供 <code>research_saturation_analyzer.py</code>、<code>ntfy_dedup.py</code> 等工具共享：</p>\n<pre><code class=\"language-python\"># tools/http_client.py — 可執行片段（官方文件 + httpx-retries 整合驗證）\nimport httpx\nfrom httpx_retries import Retry, RetryTransport\n\n\ndef make_kb_client() -&gt; httpx.AsyncClient:\n    &quot;&quot;&quot;建立 KB API 用的 AsyncClient（含重試 + 連接池）。&quot;&quot;&quot;\n    return httpx.AsyncClient(\n        base_url=&quot;http://localhost:3000&quot;,\n        timeout=httpx.Timeout(30.0, connect=5.0),\n        limits=httpx.Limits(\n            max_keepalive_connections=10,\n            max_connections=50,\n            keepalive_expiry=30,\n        ),\n        transport=RetryTransport(Retry(total=3, backoff_factor=0.5)),\n        headers={&quot;Content-Type&quot;: &quot;application/json&quot;},\n        http2=False,  # 本地 API 不支援 HTTP/2\n    )\n\n\ndef make_ntfy_client() -&gt; httpx.AsyncClient:\n    &quot;&quot;&quot;建立 ntfy.sh 用的 AsyncClient（快速失敗策略）。&quot;&quot;&quot;\n    return httpx.AsyncClient(\n        base_url=&quot;https://ntfy.sh&quot;,\n        timeout=httpx.Timeout(10.0, connect=5.0),  # ntfy 要快速失敗\n        transport=RetryTransport(Retry(total=2, backoff_factor=1.0)),\n    )</code></pre>\n<p><strong>落地場景二：研究飽和度分析工具非同步化</strong></p>\n<p><code>tools/research_saturation_analyzer.py</code> 每次執行需查詢 KB API，遷移到 <code>httpx.AsyncClient</code> 後可並行查多個搜尋關鍵字，預計降低 30-50% 執行時間。</p>\n<hr>\n<h2 id=\"完整參考書目\">完整參考書目</h2>\n<table>\n<tr><th>#</th><th>機構/作者</th><th>年份</th><th>標題</th><th>URL</th><th>source_type</th></tr>\n<tr><td>1</td><td>encode/httpx</td><td>2026</td><td>Async Support — HTTPX</td><td>https://www.python-httpx.org/async/</td><td>official</td></tr>\n<tr><td>2</td><td>encode/httpx</td><td>2026</td><td>Resource Limits — HTTPX</td><td>https://www.python-httpx.org/advanced/resource-limits/</td><td>official</td></tr>\n<tr><td>3</td><td>encode/httpx</td><td>2026</td><td>Timeouts — HTTPX</td><td>https://www.python-httpx.org/advanced/timeouts/</td><td>official</td></tr>\n<tr><td>4</td><td>encode/httpx</td><td>2026</td><td>Clients — HTTPX</td><td>https://www.python-httpx.org/advanced/clients/</td><td>official</td></tr>\n<tr><td>5</td><td>Will Ockmore</td><td>2025</td><td>HTTPX Retries Library</td><td>https://will-ockmore.github.io/httpx-retries/</td><td>official</td></tr>\n<tr><td>6</td><td>BetterStack Community</td><td>2026</td><td>Getting Started with HTTPX: Python's Modern HTTP Client</td><td>https://betterstack.com/community/guides/scaling-python/httpx-explained/</td><td>blog</td></tr>\n<tr><td>7</td><td>OneUptime</td><td>2026-02-03</td><td>How to Use httpx for Async HTTP Requests</td><td>https://oneuptime.com/blog/post/2026-02-03-python-httpx-async-requests/view</td><td>blog</td></tr>\n<tr><td>8</td><td>Miguel Mendez</td><td>2024-10-20</td><td>Httpx vs Aiohttp: Handling High-Concurrency Requests in Async Python</td><td>https://miguel-mendez-ai.com/2024/10/20/aiohttp-vs-httpx</td><td>benchmark</td></tr>\n<tr><td>9</td><td>dasroot.net</td><td>2026-02</td><td>Implementing Retry &amp; Timeout Strategies in AI APIs</td><td>https://dasroot.net/posts/2026/02/implementing-retry-timeout-strategies-ai-apis/</td><td>blog</td></tr>\n<tr><td>10</td><td>Fastio</td><td>2026</td><td>AI Agent Retry Patterns - Exponential Backoff Guide 2026</td><td>https://fast.io/resources/ai-agent-retry-patterns/</td><td>blog</td></tr>\n<tr><td>11</td><td>fooish.com</td><td>2026</td><td>Python httpx：高效能的次世代同步與非同步 HTTP 用戶端（中文）</td><td>https://www.fooish.com/python/httpx.html</td><td>blog/zh</td></tr>\n<tr><td>12</td><td>dailypypy.org</td><td>2026</td><td>httpx：Python 新世代 HTTP 客戶端完全攻略（正體中文）</td><td>https://dailypypy.org/learn/python-httpx/</td><td>blog/zh</td></tr>\n<tr><td>13</td><td>encode/httpx GitHub</td><td>2023</td><td>Issue #838: HTTPX AsyncClient slower than aiohttp?</td><td>https://github.com/encode/httpx/issues/838</td><td>community</td></tr>\n<tr><td>14</td><td>e2b-dev/E2B GitHub</td><td>2024</td><td>Issue #1155: httpx client connection pool leaks on sandbox kill</td><td>https://github.com/e2b-dev/E2B/issues/1155</td><td>community</td></tr>\n</table>\n","headings":[{"level":2,"text":"技術概述","slug":"技術概述"},{"level":2,"text":"核心概念與原理","slug":"核心概念與原理"},{"level":3,"text":"1. 傳輸架構（Transport Layer）","slug":"1-傳輸架構-transport-layer"},{"level":3,"text":"2. 連接池管理（Connection Pooling）","slug":"2-連接池管理-connection-pooling"},{"level":3,"text":"3. 四維超時系統（Timeout System）","slug":"3-四維超時系統-timeout-system"},{"level":3,"text":"4. AsyncClient 生命週期管理","slug":"4-asyncclient-生命週期管理"},{"level":3,"text":"5. HTTP/2 多路複用","slug":"5-http-2-多路複用"},{"level":2,"text":"最佳實踐","slug":"最佳實踐"},{"level":3,"text":"1. 應用層級單一客戶端（Single Client Instance）","slug":"1-應用層級單一客戶端-single-client-instance"},{"level":3,"text":"2. 結構化超時配置策略","slug":"2-結構化超時配置策略"},{"level":3,"text":"3. httpx-retries 聲明式重試（網路層）","slug":"3-httpx-retries-聲明式重試-網路層"},{"level":3,"text":"4. tenacity + httpx 處理 HTTP 429 Rate Limit","slug":"4-tenacity-httpx-處理-http-429-rate-limit"},{"level":3,"text":"5. asyncio.gather 並行請求模式","slug":"5-asyncio-gather-並行請求模式"},{"level":2,"text":"陷阱與反例","slug":"陷阱與反例"},{"level":3,"text":"陷阱一：熱路徑建立多個客戶端（P0 — 最常見）","slug":"陷阱一-熱路徑建立多個客戶端-p0-最常見"},{"level":3,"text":"陷阱二：忘記 aclose() 造成資源洩漏","slug":"陷阱二-忘記-aclose-造成資源洩漏"},{"level":3,"text":"陷阱三：超高並發場景 ReadError（&gt;300 concurrent）","slug":"陷阱三-超高並發場景-readerror-gt-300-concurrent"},{"level":3,"text":"陷阱四：timeout=None 導致 AI Agent 無限掛起","slug":"陷阱四-timeout-none-導致-ai-agent-無限掛起"},{"level":2,"text":"與本專案關聯","slug":"與本專案關聯"},{"level":2,"text":"完整參考書目","slug":"完整參考書目"}],"prev":{"slug":"httpx-傳輸層重試與連線池長生命週期-asynccli-728f45b7","title":"httpx 傳輸層重試與連線池：長生命週期 AsyncClient 取代腳本 curl（2026-05-01 技術研究）"},"next":{"slug":"mem0mem0aimem0-544k-stars-acaaa3d8","title":"Mem0（mem0ai/mem0）— 54.4k Stars 生產級 AI Agent 通用記憶層：混合存儲架構、2026 演算法突破與 daily-digest 落地方案"}}